# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

AAanalysis is a Python framework for interpretable, sequence-based protein
prediction. Production/Stable on PyPI, BSD-3, semver-strict from v1.x onward.

## Path-scoped rules

The repo uses `.claude/rules/` for guidance that applies only to specific
paths. This CLAUDE.md keeps the always-loaded core; the rules below
auto-load when Claude reads matching files:

| Rule file | Loads when touching |
|---|---|
| `code-conventions.md` | any `aaanalysis/**/*.py` (skeleton, naming, type hints, lint, Python floor, `ut.X` constants) |
| `frontend-backend.md` | any `aaanalysis/**/*.py` (signatures, validation block, backend-trusts-frontend) |
| `pro-core-boundary.md` | any `aaanalysis/**/*.py` and `aaanalysis/__init__.py` (`*_pro` gating, `missing_feature_stub`, parity) |
| `reproducibility.md` | any `aaanalysis/**/*.py` (`random_state` / `seed` contract) |
| `errors-warnings-logging.md` | any `aaanalysis/**/*.py` or `tests/**/*.py` |
| `docstrings.md` | `aaanalysis/**/*.py`, `docs/source/**/*.rst` |
| `backend-dataframes.md` | `aaanalysis/**/_backend/**/*.py` |
| `plotting.md` | `aaanalysis/plotting/**`, files matching `*plot*.py` / `*Plot*.py` |
| `config.md` | `aaanalysis/config.py` |
| `testing.md` | `tests/**/*.py` |
| `dependencies-and-pyproject.md` | `pyproject.toml` |
| `api-stability.md` | `aaanalysis/__init__.py` |
| `changelog.md` | `CHANGELOG.md` (file does not exist yet — aspirational) |
| `dev-scripts.md` | `dev_scripts/**` |
| `notebooks.md` | `examples/**/*.ipynb`, `tutorials/**/*.ipynb` (display_df tables, `plt.show()`/`tight_layout()` plots, executed outputs) |

## Other reference rules (no auto-load)

These are not path-scoped — open them when the topic comes up.

- `.claude/rules/workflow.md` — running tests/docs locally, git / PR workflow
- `.claude/rules/repo-layout.md` — directory tree, class templates, extras overview
- `.claude/rules/sharp-edges.md` — accepted architectural limitations not to "fix"

---

## 0. Hard rules (no exceptions)

These rules override any later guidance. Per-task user approval is required;
prior approvals do not carry over to future tasks or sessions.

1. **Never delete any file without explicit user permission.** This applies to
   every mechanism: `rm`, `git rm`, `Write` overwrites that erase content,
   `git clean`, `git reset --hard` that drops untracked files, `git checkout
   -- <path>` that discards uncommitted work, IDE/tool deletions, branch
   deletion (`git branch -D`), and any script or command whose effect is to
   remove a file. If unsure whether an action deletes a file, ask first.
   Moving or renaming a file (`git mv`, `mv`) also requires permission since
   the original path disappears. "The file looks unused / stale / autogenerated"
   is not sufficient justification — ask.
2. **Never push to GitHub without explicit user permission.** This applies to
   `git push` in all forms (including `--force`, `--force-with-lease`,
   `--tags`, pushing new branches, pushing to forks), `gh pr create` /
   `gh pr merge` / `gh release create`, and anything else that publishes
   commits, tags, branches, PRs, or releases to a remote. Local commits are
   fine without asking; anything that leaves the local repo is not.

If the user gives permission for a specific deletion or push, that
authorization is scoped to that exact action only. Ask again for the next
one.

---

## 1. Quick orientation

- The public API is exactly what `aaanalysis/__init__.py` re-exports.
  Anything starting with `_` is private and may change without deprecation.
- `aaanalysis/utils.py` is the central import barrel. Internal modules do
  `import aaanalysis.utils as ut` and access constants (`ut.COL_*`,
  `ut.COLOR_*`, `ut.LIST_*`), validators (`ut.check_*`), and shared
  helpers through it. Domain-bundle constants are **defined in `utils.py`,
  not in subpackage files** — full rule in `code-conventions.md`. Backend
  code MUST go through `ut`, never reach into `aaanalysis._utils.*`
  directly.
- Class templates, plot-pair convention, and `pro` / `docs` / `dev` extras
  are summarised in `repo-layout.md`.

---

## 2. Confirmation policy (CONFIRM-FIRST surfaces)

Claude **always asks the user before** editing any of these:

1. `pyproject.toml` (deps, version, build config)
2. `aaanalysis/__init__.py` (public API re-exports)
3. Any rename or deletion of a symbol in `aaanalysis.__all__`
4. Any file in `aaanalysis/_data/` (bundled scales, datasets, benchmarks)
5. `.github/workflows/*` (CI configuration)
6. `aaanalysis/config.py` (options surface)
7. `aaanalysis/template_classes.py` (Wrapper / Tool ABCs)

The §0 hard rules (file deletion, pushing to a remote) apply on top of this
list. Authorization stands per change, not per session.

---

## 3. Common anti-patterns (always-loaded reminder)

The detailed rules live in the path-scoped files above; these are the
highest-risk traps worth keeping front-of-mind every session.

- **No `print(...)` in library code.** Use `ut.print_out(...)`.
- **No ADR references in project code or GitHub (hard rule).** Never cite an
  ADR (`ADR-0001`, "see ADR-0001", a `docs/adr/...` path) in **any `.py` file**
  (docstrings **and** `#` comments, library **and** test) or in **GitHub**
  content (issue bodies, PR descriptions/titles, review comments). ADRs change /
  get superseded and aren't part of the published RTD docs or the user-facing
  GitHub surface, so a pointer goes stale and leaks internal process. State the
  rationale in plain language instead. ADR cross-references stay confined to the
  decision layer: `docs/adr/`, `CONTEXT.md`, `.claude/rules/`, and dev guides
  under `docs/guides/`.
- **Notebook presentation (examples + tutorials).** In `.ipynb` cells, show
  tables with `aa.display_df(df, n_rows=10, show_shape=True)` — never a bare
  `df`/`df.head()` or `print(df)`. End every plot cell with `plt.tight_layout()`
  then `plt.show()` (inline backend) so the figure actually renders — a bare
  `Plot().method(...)` leaves only a useless `<Axes …>` text repr in the output.
  This does **not** change library code, which still never calls `plt.show()` /
  `plt.tight_layout()` (those live only in notebook/user code; see
  `notebooks.md` and `plotting.md`).
- **No imports from `aaanalysis._utils.*` outside `utils.py`.** Go through
  `import aaanalysis.utils as ut`.
- **No bare `except Exception:` to silence errors.** Let them surface.
- **No custom exception base** (`AAanalysisError` etc.) — use bare
  `ValueError` / `RuntimeError`.
- **No cross-class backend imports.** A frontend imports backend helpers only
  from a shared `_backend/*.py` module (top-level) or its *own* dedicated
  `_backend/<subpkg>/` — never from another class's dedicated subpackage (e.g.
  `SequenceFeature` reaching into `_backend/num_feat/`). Put shared helpers in a
  common `_backend/*.py`. Enforced by
  `tests/unit/api_tests/test_backend_import_hygiene.py`.
- **In example/tutorial notebooks, show DataFrames with
  `aa.display_df(df, n_rows=10, show_shape=True)`** — never `print(df)`, a bare
  `df`, or `df.head()`. `display_df` renders a clean, width-bounded table and the
  shape, which is the house presentation for every `examples/**` and
  `tutorials/**` notebook.
- **Parallel agents must not share one checkout.** When more than one agent or
  process works in this repo at once, give each its own per-task `git worktree`
  (see `workflow.md`). A shared working tree / `HEAD` lets a concurrent commit or
  push land *between* your `git status` and your commit — so re-check status
  immediately before staging, commit **explicit pathspecs** (never a blind
  `git add -A` / `git commit -a`), and never commit, revert, or discard changes
  you did not make. If unexpected edits appear in the tree, stop and surface them
  rather than bundling them into your commit.
