# AAanalysis docstring house style — reference (pointer)

> **Moved.** The canonical, authoritative docstring guide — class/method/function
> templates, recurring-parameter baselines, citation rules, the self-contained-
> parameters rule, cross-reference integrity, versioning & deprecation, examples
> & verification, glossary cross-links, and the full conformance checklist with
> the rule → checker-code mapping — lives in the published **Docstring Style
> Guide**:
>
>     docs/source/index/docstring_guide.rst
>
> (rendered on Read the Docs). It is the single source of truth; this skill only
> *enforces* it via `scripts/check_docstrings.py` and surfaces doc-vs-code drift
> via `scripts/doc_signature_drift.py`. Do not restate rules here — edit the guide
> instead so the prose and the checkers can never conflict.

Gold-standard exemplars to mirror: `CPP`, `AAclust`, `dPULearn`, `CPPPlot`.
