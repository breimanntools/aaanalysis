# AAanalysis docstring house style — reference (pointer)

> **Moved.** The canonical, authoritative docstring guide — class/method/function
> templates, recurring-parameter baselines, citation rules, versioning &
> deprecation, examples & verification, glossary cross-links, and the full
> conformance checklist with the rule → checker-code mapping — now lives in the
> published **Docstring Style Guide**:
>
>     docs/source/index/docstring_guide.rst
>
> (rendered on Read the Docs). It is the single source of truth; this skill only
> *enforces* it via `scripts/check_docstrings.py`. Do not restate rules here —
> edit the guide instead so the prose and the checker can never conflict.

Gold-standard exemplars to mirror: `CPP`, `AAclust`, `dPULearn`, `CPPPlot`.
