# GitHub issues — implementation handoff

> **Post-merge update — 2026-06-14 (this session):** three overnight-lane PRs merged to `master`,
> closing **#130** (`single_logo` from `df_parts`, PR #221 `ea275f6f`) and **#88** (security hardening
> audit, PR #224 `300e9bc0`). **#134** (inherit Wrapper/Tool ABCs, PR #220 `e31c0fd1`) is **partial /
> kept open** — `TreeModel`/`ShapModel`/`dPULearn`/`AAMut` are now bound to the ABCs + meta-test, but the
> `SeqMut.run` decision is unresolved (it auto-closed on merge by accident — the PR body literally
> contained the text "Closes #134"; reopened). **Two lane PRs are still open, awaiting review — do NOT
> develop their issues in parallel:** **PR #222** (#127 + #214, `CPPPlot.profile`/`feature_map`
> `df_seq=`/`sample=`) and **PR #223** (#211 + #24, sklearn `Pipeline` proof). New issue since last
> refresh: **#219** (dense-plot row-label overlap gate, v1.1/prio:2). Open count **61 → 60**.

_Generated by the `github-issues` skill; last refreshed **2026-06-14** (open count 63 → **61**:
+3 new — **#210** (epic: ecosystem integration), **#211** (sklearn `Pipeline` compat proof), **#214**
(CPPPlot `sample=`/`df_seq=` SHAP-plot sugar); −5 net closed since last refresh — **#81** (cheat sheet),
**#129** (ShapModel fuzzy soft labels), **#157** (dPULearn 1/0 labels + `n_neg`), **#158** (AAclustPlot
plot-from-fitted), plus the entire **perf sweep** #180/#186/#187/#188/#199/#200/#201. **All 61 open issues
now carry a milestone (v1.1–v2.X)** — see the Milestones section). Regenerate:_
_`python3 .claude/skills/github-issues/scripts/fetch_issues.py`, then re-run the skill._

Audits every open issue against the package scope (interpretable, CPP-centered, sequence-based
protein prediction; `pro` extra for heavy deps; semver-strict v1) and the coding standards in
`CLAUDE.md` + `.claude/rules/sharp-edges.md`. Verdicts: ✅ Ready · 🔄 Revisit (needs a decision /
under-specified / oversized) · ⏸️ Defer-v2 · ❌ Reject (rule conflict) · ☑️ Done/Partial.

## Snapshot
- **Open issues: 60 · closed: 56.** Verdict mix (open): ✅ ~15 · 🔄 ~43 · ⏸️ 1 (#64) · ❌ 0 · ☑️ 2 (#37 + #134 both kept-open-partial).
- **Just merged this session (3 PRs):** **#130** ☑️ closed (PR #221 `ea275f6f`) · **#88** ☑️ closed (PR #224 `300e9bc0`) · **#134** ☑️ *partial, kept OPEN* (PR #220 `e31c0fd1` — ABCs bound on TreeModel/ShapModel/dPULearn/AAMut + meta-test; **`SeqMut.run` decision still pending**). **In-flight (open PRs, awaiting review — serialize, don't parallel-develop):** **#127+#214** → PR #222 · **#211+#24** → PR #223.
- **New since last refresh (1):** **#219** ✅ — dense-plot row-label **overlap test gate** + shrink-to-floor-then-grow auto-sizing for `feature_map`/`heatmap`/`ranking`/`profile`; routes font scaling through `plot_settings`. v1.1, prio:2, well-specified with KPIs. **Overlaps PR #222** (same `_cpp_plot.py` methods) + the doc/cheat-sheet lane → serialize behind #222.
- **Milestone coverage: 100%** — every open issue is assigned (v1.1: 18 · v1.2: 4 · v1.3: 14 · v1.4: 10 ·
  v1.5: 5 · v2.X: 10). The 8 previously-unmilestoned issues (#44, #126, #127, #130, #131, #132, #133, #134)
  were assigned this pass; #214 was re-homed v1.2→v1.1 (it's API-ergonomics, not protein design), and the
  ecosystem epic #210 + its sklearn child #211 were re-homed v2.X→v1.4 (integrations theme). See Milestones.
- **New since last refresh (3):**
  - **#210** 🔄 — *epic: ecosystem integration* (consume upstream + descriptors, expose downstream). Memory
    anchor; children spun out later (sklearn proof = #211, adapter notebooks, descriptor consumption, Captum/
    Quantus wrap). prio:3, v1.4. Don't "implement #210" — drive children.
  - **#211** ✅ — prove + document sklearn `Pipeline` compat: wrap stateless `feature_matrix` in a stock
    `FunctionTransformer`, integration test + byte-identical regression + recipe notebook, **no new public
    symbol**. Child of #210; overlaps #24 (ML-pipeline wrapper) — same lane. prio:3, v1.4.
  - **#214** ✅→🔄 — `CPPPlot.profile`/`feature_map` accept `sample=`/`df_seq=` to resolve the `feat_impact_*`
    column + seq parts internally (SHAP plots). **Overlaps #127** (both add `df_seq=` to the same two
    methods) → serialize. Child of #126; prio:3, v1.1.
- **Recently resolved on master (verify before closing):**
  - **#81** ☑️ closed 2026-06-13 — AAanalysis cheat sheet (single-source HTML+PDF under `docs/_cheatsheet/`;
    ongoing polish commits land directly, e.g. `33cc112b`).
  - **#129** ☑️ closed 2026-06-13 — `ShapModel.fit` entry-keyed soft labels (`fuzzy={'P05067': 0.6}`).
  - **#157** ☑️ closed 2026-06-13 — `dPULearn.fit` accepts 1/0 labels (`label_pos`/`label_unl`) + `n_neg`
    alias (`b831b2d6`, PR #166); cheat-sheet recipe updated to the merged API (`0e241f08`).
  - **#158** ☑️ closed 2026-06-13 — `AAclustPlot.centers`/`medoids` plot directly from a fitted `AAclust`.
  - **Perf sweep all closed** (same-output unless noted): **#180** encode_pdb alignment cache + pro-IO fetch
    concurrency (`2fcec82a`/`9fb8f0c9`); **#186** Batch-6 vectorization tail; **#187** committed benchmark +
    regression-guard suite; **#188** numerical-equivalence tolerance policy (**ADR-0032**, `d6eaf1be`);
    **#199** opt-in heuristic CPP.simplify pruning (T3, exact default); **#200** opt-in binary-search
    `AAclust.fit` k (T3, exact default); **#201** encode_pdb shared coord/residue cache (byte-identical).
  - Prior refresh closures still relevant context: **#74** CHANGELOG + `deprecated()` decorator
    (`29f886e7`, PR #153), **#135** docstring contracts (`5a85193e`), **#21** prediction-tasks concept page
    (`55528957`), **#20** `get_feature_descriptions` (`5f4177d9`), **#43**/**#128** AAclust selection.
  - **#37** — ☑️ merged (`33248426`+`0c090bff`); **issue intentionally LEFT OPEN for author review.**
    Was the gate for the protein-design chain #57/#59/#60 — gate **LANDED**, chain **UNBLOCKED**.
- **`deprecated()` machinery available** (`aaanalysis/_utils/decorators.py`, reachable via `ut`, from #74):
  the breaking/alias children **#132 / #133** land directly against it + CHANGELOG — no machinery to build.
- Untracked-by-issue work also landed recently (verify, don't re-do): **`EmbeddingPreprocessor`**
  (`fetch_embeddings` + `pool_embeddings`, new **`[embed]` extra**, ADR-0029, `ed83c271` PR #145) — this
  partially addresses the **embedding cluster (#22/#23)**; `add_feat_importance(sort=)` + `CPPPlot.feature(feat_rank=)`
  (`12b887b2`, PR #136); CPP data-representations tutorial (`ee8d5fa0`, PR #156); integration + e2e test
  tiers (`29924665`, #173).

## Milestones (release scoping — semver-strict v1.x; **100% assigned as of 2026-06-14**)

Organizing principle: **AAanalysis ships interpretable scientific primitives; ProtXplain orchestrates
them into campaigns/loops.** Milestones complement the `prio:` labels (milestone = *which release*;
prio = *how soon*). Every open issue is now assigned on GitHub; counts below match the live milestone
state (`gh api repos/:owner/:repo/milestones`). Re-triage as work lands.

- **v1.1 — release-readiness: docs onramp + quick wins + API trust** (18 open):
  #35, #69, #75, #76, #77, #80, #106, #107, #108, #109, #119, #120 (pro structure plots — see note),
  #219 (dense-plot overlap gate), #126 (epic) + additive children #127, #131, #134 (partial), #214.
  ~~#130~~ merged (PR #221).
  ~~#18 #20 #21 #74 #78 #81 #82 #84 #135 #157 #158~~ done.
- **v1.2 — protein design** (4 open; #37 gate landed): #16, #57, #59 (+ #60 in v1.4). ~~#37 #58 #61~~ done/closed.
- **v1.3 — core CPP feature & evaluation story** (13 open): #25, #26, #27, #28, #29, #33, #36, #87,
  #89, #91, #93, #132, #133. ~~#32 #43 #88 #128~~ done/closed (#88 = PR #224).
- **v1.4 — integrations & advanced primitives** (10 open): #22, #23, #24, #40, #45, #56, #60, #64,
  **#210** (ecosystem epic), **#211** (sklearn-Pipeline proof). _(embedding input path #145/ADR-0029 landed.)_
- **v1.5 — heavier work** (5 open): #42, #46, #62 (GPU/parallel — only open perf issue; #19 perf closed by the sweep), #65, #79.
- **v2.X — XAI suite (ProtXplain-scoped) + research/deferred** (10 open): #44 (time-resolution, ADR-gated),
  #47, #48, #49, #50, #51, #52, #53, #54, #55.

**Re-homings this pass (documented):** #214 v1.2→v1.1 (API-ergonomics sugar, not protein design);
#210 + #211 v2.X→v1.4 (parent/child kept together under the integrations theme — both additive, #211 ships
with no new public symbol); #44 → v2.X (research-y, ADR-gated). The previously-unmilestoned API-ergonomics
children of #126 (#126/#127/#130/#131/#132/#133/#134) are now placed by breaking-ness: additive → v1.1,
deprecation-alias (#132/#133) → v1.3.

> **Pro structure plots #119 (prio:1) / #120** sit in **v1.1** on GitHub, but they open a new
> `feature_engineering_pro/` subpackage (CONFIRM-FIRST). Treat as a self-contained pro lane (Lane I) that
> can land in v1.1 or slip — re-confirm scope before starting; not a blocker for the rest of v1.1.

## Priority tiers & Kanban (2026-06-14)

`prio:` labels drive the board's Status column: **`prio:1` → "To Do"** · **`prio:2`/`prio:3` →
"Backlog"** · closed → "Done". Every open issue carries exactly one `prio:`. Current split —
**7 / 18 / 35** (60 open):

- **prio:1 (7 — To Do):** #16, #37 (kept open for review), #57, #59, #91, #106, #119. _(#81 cheat sheet closed.)_
- **prio:2 (18 — headline-next):** #22, #28, #29, #33, #35, #40, #56, #60, #80, #107,
  #109, #120, #126, #127, #131, #132, #134 (partial), #219. _(#88 closed — PR #224.)_
- **prio:3 (35 — convenience / extensions / research / heavy-deferred):** #23, #24, #25, #26,
  #27, #36, #42, #44, #45, #46, #47–#55, #62, #64, #65, #69, #75, #76, #77, #79, #87, #89, #93,
  #108, #133, #210, #211, #214. _(#130 closed — PR #221.)_

## ▶ Next 3 to work on (parallel-safe — different subsystems, no shared files)

| # | prio | lane / files | why now | the decision to settle first |
|---|---|---|---|---|
| **57** | 1 | C — `protein_design/` | **unblocked (#37 landed)**; ML-guided directed evolution builds on the merged `SeqMut.suggest`/ΔCPP engine | feature-importance → ranked minimal mutation set; campaign-looping → ProtXplain |
| **91** | 1 | G — `metrics/` + new eval surface | prio:1, self-contained (reuses `aaanalysis.metrics` + `comp_bootstrap_ci`); #93 builds on it | helpers vs `Tool`-class+Plot vs hybrid (#91 design options) |
| **29** *(quick win)* | 2 | A — `feature_engineering/` schema | ✅ Ready, unblocked (#18 schema landed); additive z-score/min-max normalization appended via `sort_cols_feat`'s tolerant tail | none material — serialize ahead of #33/#26 (same `LIST_COLS_FEAT` contract, one owner) |

**#134's remaining sliver** is now the only open part (the ABC inheritance + meta-test merged in PR #220):
decide whether `SeqMut` gains a `.run` method (→ inherit `Tool`) — a design call, not a quick win; bring it
to a review session.

Alternatives with momentum: the deprecation-alias children **#132**/**#133** land directly against #74's
`deprecated()` helper (each needs its decision settled first). Also **#106** docs onramp (Lane E) and
**#119** (prio:1 but a whole new pro subpackage — bigger; Lane I). **In review now — don't restart:**
#127/#214 (PR #222) and #211/#24 (PR #223). _(#130 + #88 merged this session; #157/#158/#129 closed earlier.)_

---

## 🌙 Overnight parallel sessions (unattended-safe set) — added 2026-06-14

For running multiple sessions overnight with **no mid-flight check-ins**, the bar is stricter
than ✅ Ready: a lane must (a) need **no design decision**, (b) **not stall on a CONFIRM-FIRST
surface** (`__init__.py` / `pyproject.toml` / `config.py` / `_data/` / `template_classes.py`),
and (c) be **file-disjoint** from every other lane. Each session runs in its **own
`git worktree`** (mandatory for parallel agents — `workflow.md`), implements to the issue KPIs,
runs targeted tests with `-c tests/pytest.ini`, commits **locally with explicit pathspecs**, and
**stops at the push gate** (hard rule §0 — no push without per-action approval). Review + push in
the morning.

**Verified all five are method-additive on existing classes → none adds a public symbol → none
hits the `__init__.py` wall:** `single_logo` (`_aalogo_plot.py:241`), `profile`/`feature_map`
(`_cpp_plot.py:713`/`:1194`), `print_out` (`_utils/utils_output.py:63`), Wrapper/Tool
(`template_classes.py:7`/`:34`).

**Outcome (2026-06-14):** all five lanes ran and opened PRs #220–#224. **Merged:** N1 #134 → PR #220
(partial — ABCs bound; `SeqMut.run` left), N2 #130 → PR #221, N5 #88 → PR #224. **Open, awaiting review
(do not restart):** N3 #127/#214 → PR #222, N4 #211/#24 → PR #223.

| Lane | Issue(s) | Status | Disjoint files | Decision-free? | Notes |
|---|---|---|---|---|---|
| **N1** | **#134** | ✅ merged PR #220 (partial) | model class defs (`dpulearn`/`treemodel`/`shapmodel`/`aamut`/`seqmut`) + new `tests/unit/api_tests/` meta-test | ✅ | ABCs bound on TreeModel/ShapModel/dPULearn/AAMut + meta-test. `SeqMut.run` decision still open → issue kept open. |
| **N2** | **#130** | ✅ merged PR #221 | `seq_analysis/_aalogo_plot.py` (+ its `_backend/_aalogo/`) + test + its own notebook | ✅ | `single_logo(df_parts=, labels=, label_test=, tmd_len=)`; frames via AAlogo getters through the frontend; regression test vs the two-getter path. Cleanup added a `tmd_len`-without-`df_parts` guard. |
| **N3** | **#127 → #214** | 🔄 open PR #222 | `feature_engineering/_cpp_plot.py` (both methods) + tests | ✅ | **Same file — ONE session, serialize #127 then #214.** Keep `**args_seq`/`col_imp=` back-compat. Review flagged a `sample`-without-`df_seq` ordering bug — fix before merge. |
| **N4** | **#211 (+#24)** | 🔄 open PR #223 | new integration test + recipe notebook (no library edit) | ✅ | Wrap stateless `sf.feature_matrix` in a stock `FunctionTransformer`; `Pipeline([feats, RF])` round-trip + **byte-identical** regression. Review flagged a leaked worktree path in the committed notebook output — re-run before merge. |
| **N5** | **#88** | ✅ merged PR #224 | audit notes + missing `timeout=` in `data_handling_pro/_backend/.../_comp_seq_cons.py` | ✅ | Audit + small hardening (added `get_msa` timeout) + AST timeout-hygiene meta-test. **NOT a SECURITY.md** (sharp-edges reject). |

**Lower-value extras (safe but optional):** #108 (tutorial "You will learn" box — docs only) ·
#75 (route output through logging, keep `print_out` shim — internal, but changes output broadly,
slightly riskier). Both edit notebooks/output paths, so keep them out of N2/N3's notebook scope.

**Excluded from overnight (need you awake):** #91, #57, #59, #131, #132, #133 (design decision) ·
#33, #119 (CONFIRM-FIRST `__init__.py`/`pyproject.toml`/`config.py` stall) · #29 (borderline — safe
only if scoped to an additive `NumericalFeature` method with **no** new export).

**Doc-collision guard:** N2, N3, #108, #75 each touch notebooks — give each its **own** notebook;
never let two lanes edit the same `.ipynb`.

---

## Implementation order (do top-down; respect blockers)

1. **#57** — ML-guided directed evolution (prio:1; unblocked). Lane C; serialize ahead of #59 → #60.
2. **#91** — model evaluation & comparison (prio:1). Lane G; settle helpers vs `Tool`-class+Plot vs hybrid first. #93 builds on it.
3. **API-ergonomics quick wins** (Lane H, additive, no breaking): **#134** ABCs **merged** (PR #220) —
   only the `SeqMut.run` design decision remains · **#127** + **#214** **in review** (PR #222; one owner,
   serialize; fix the `sample`-without-`df_seq` ordering bug before merge) · **#130** `single_logo`
   **merged** (PR #221). _(#88 + #130 merged this session; #157/#158/#129 already shipped.)_
4. **Docs onramp** (Lane E): **#106** (split into #107/#108/#109 first) → #69 (retarget first).
   _(#20/#21/#81 children now closed; #81 cheat sheet shipped.)_
5. **API-ergonomics decisions-first** (Lane H, serialize the breaking ones): **#131** (config
   CONFIRM-FIRST) → **#132** (label-name unification, deprecation alias) → **#133** (plot-return
   contract — needs a new ADR). **#74 has landed** — the `deprecated()` decorator + CHANGELOG already
   exist, so #132/#133 land directly against them (no machinery to build first).
6. **Schema consumers** (Lane A): #29 (normalization), #33 (export), #26 (ProtXplain contract) — build
   on the frozen `LIST_COLS_FEAT` + `sort_cols_feat`.
7. **Pro structure plots** (Lane I, serialize): **#119** `map_structure` → **#120** `interactive`.
   New `feature_engineering_pro/` subpackage; CONFIRM-FIRST (`__init__.py`, `pyproject.toml`, new `ut`
   colour constants). _(#129 pro fuzzy labels already shipped.)_
8. **Revisit/deferred clusters:** structure/conservation (#40/#64/#65/#42), embedding (#22/#23/#47),
   XAI long-tail (#47–56), perf (#62 — GPU/parallel, only one left), the #67 v2 umbrella tail.

## Parallel lanes (safe to run in separate sessions — no shared files)

- **Lane A — CPP core/schema:** schema **consumers** #29 / #33 / #26 (build on `LIST_COLS_FEAT` +
  `sort_cols_feat`; one owner). ~~#17 #18 #61~~ done.
- **Lane B — Performance:** **#62** (GPU/parallel; opt-in, CPU default) is the only open perf issue.
  ~~#19 #30~~ done (the 2026-06 same-output sweep closed #19/#180/#186/#187/#188/#199/#200/#201).
- **Lane C — Protein design (SERIALIZE internally):** **{#57 → #59 → #60}** on the merged AAMut/SeqMut
  + ΔCPP workflow. ~~#37 #58~~ done/closed.
- **Lane D — Data/sampling:** #25, #28 (builds on the landed `AAWindowSampler`). ~~#66 #32~~ done.
- **Lane E — Docs:** #106 (parent) → #107/#108/#109 children · #69, #35, #80. Doc files
  only; independent of code lanes. **Coordinate with Lane H** where children edit tutorials/cheat sheet.
  _(#20/#21 children closed.)_
- **Lane F — Structure/conservation (pro, SERIALIZE):** #65 → #40/#42; **#64 deferred (ADR-0012).**
  Share `data_handling_pro` + the pending pro-package move.
- **Lane G — Evaluation:** #91 → #93. Build on `aaanalysis.metrics` + `comp_bootstrap_ci`; no shared
  files with A/C, so parallel-safe.
- **Lane H — API ergonomics (#126 epic, mixed):** additive children #130 (**merged** PR #221) and #134
  (ABCs **merged** PR #220; `SeqMut.run` decision left) are done/partial. **#127 + #214 are in review**
  (PR #222) — they add `df_seq=`/`sample=` to `CPPPlot.profile`/`feature_map`; **#219** edits the same
  `_cpp_plot.py` surface (dense-plot overlap gate) → **serialize #219 behind PR #222**, one owner. The
  breaking children #131/#132/#133 each need a decision (config option / alias / ADR); **#74's
  `deprecated()` helper + CHANGELOG already exist** so they no longer block on machinery, just on the
  decision. **Doc collision:** #127/#131/#214/#219 all update tutorials + the cheat sheet → one notebook
  owner; don't edit the same notebook concurrently. _(#130 + #88 merged; #157/#158/#129 shipped.)_
- **Lane I — Pro structure plots (NEW, SERIALIZE):** #119 `map_structure` → #120 `interactive`. New
  `feature_engineering_pro/`; CONFIRM-FIRST surfaces. _(#129 pro fuzzy labels already shipped.)_

## Overlap clusters — DO NOT develop in parallel
| Cluster | Issues | Primary | Why they collide |
|---|---|---|---|
| CPP perf loop | #62 | #62 | edits `_backend/cpp/**`; #19 + the same-output sweep already closed |
| Output schema | #29, #33, #26 | #29 | serialize behind the frozen `LIST_COLS_FEAT`/`sort_cols_feat` contract |
| Protein design | #57 → #59 → #60 | #57 | shared mutation API in `protein_design/`; #37 gate landed |
| Structure/conservation/MSA | #40, #64, #65, #42 | #65 | shared `data_handling_pro` + pending move |
| **Pro structure plots (NEW)** | #119, #120 | #119 | #120 **blocked-by** #119; share `feature_engineering_pro/` + StructureView (ADR-0028) |
| Embedding | #22, #23, #47 | #22 | shared embedding-integration surface (input path #145/ADR-0029 landed) |
| **CPPPlot `_cpp_plot.py`** | #127, #214, #219 | #127 (PR #222) | #127/#214 (`df_seq=`/`sample=`) **in PR #222**; **#219** (dense-plot row-label overlap gate) edits the same `feature_map`/`profile` surface → serialize #219 behind PR #222, one owner |
| **API-ergonomics docs** | #127, #131, #214, #219 | — | additive code is per-file independent, but all edit tutorials + the cheat sheet → one doc owner _(#130 closed — PR #221)_ |
| **API-ergonomics breaking** | #131, #132, #133 | #131 | each needs a decision (config option / alias / ADR); **#74 deprecation machinery now exists** (closed), so serialize on the *decision*, not the helper |
| **Ecosystem integration** | #210, #211, #24 | #210 | #210 = epic (don't build directly); #211 (sklearn `Pipeline` proof) overlaps #24 (ML-pipeline wrapper) → one owner |
| Feature-map plotting | #47, #56 | #47 | shared `plot_feature_map`; build on the shipped #63 `shap_plot` bars |

---

## Per-issue audit

### Recently closed (resolved on master — context only)
- **This session (2026-06-14):** **#130** `ea275f6f` (PR #221 — `single_logo` builds frames from
  `df_parts`+`labels`) · **#88** `300e9bc0` (PR #224 — subprocess/network/file hardening audit + AST
  timeout-hygiene meta-test). **#134** `e31c0fd1` (PR #220) is **partial → kept OPEN** (Wrapper/Tool ABCs
  bound on TreeModel/ShapModel/dPULearn/AAMut + meta-test; the `SeqMut.run` decision is unresolved).
- **Since 2026-06-13:** **#129** (ShapModel fuzzy soft labels) · **#157** `b831b2d6` (dPULearn 1/0 labels +
  `n_neg`) · **#158** (AAclustPlot plot-from-fitted) · **perf sweep** #180/#186/#187/#188 (**ADR-0032**
  tolerance policy)/#199/#200/#201/#19 (same-output vectorization + opt-in T3 approximations, exact default).
- **Prior refreshes:** **#74** `29f886e7` (CHANGELOG + `deprecated()` decorator) · **#135** `5a85193e`
  (docstring contracts) · **#21** `55528957` (Prediction-tasks concept page) · **#20** `5f4177d9`
  (`get_feature_descriptions`) + `8ad0dc0d` (`versionadded`) · **#43** `94d9b0bc`+`5e380c14` (AAclust
  protein-selection + CD-HIT comparison) · **#128** `f15a8e9d`+`48617c9c` (`select_scales`/`select_proteins`) ·
  **#82** `362cb4ea` · **#78** `56670996` · **#84** `32847f06` · **#32** model-free pruning + `TreeModel.select_features` ·
  **#61** multi-class/regression label helpers + P8 · **#37** AAMut/SeqMut (merged; **issue kept OPEN for
  review**) · **#66/#86/#90/#83/#63/#92/#94/#95/#30/#17/#18/#31/#71/#72/#73** (earlier).

### API-ergonomics epic #126 (new) — `aaanalysis/` mixed
| # | prio | verdict | scope / standards | already-addressed | implementation note (complements the issue) |
|---|---|---|---|---|---|
| 126 | 2 | 🔄 | Tracking umbrella; fits | Partial (#128 done) | Don't "implement #126" — it's a parent. Drive children individually; each = one deliverable + KPI. Constraint: semver-strict, all defaults byte-identical. |
| 127 | 2 | ✅ | Additive; fits; `_cpp_plot.py` | **🔄 IN PR #222** (open, awaiting review) | Add `df_seq=`/`entry=` to `feature_map`+`profile`; derive `*_seq` via `SequenceFeature.get_df_parts`; frontend errors if both `entry` and explicit `*_seq`. Regression test vs the `**args_seq` path. Edits `tutorial5a`+cheat sheet (doc-collision lane). **Serialize #214 + #219 behind this.** |
| 128 | 2 | ☑️ | **DONE / closed** (`f15a8e9d`+`48617c9c`) | Yes | Shipped as `AAclust.select_scales` + `AAclust.select_proteins` (both verified in `_aaclust.py`). |
| 129 | 3 | ☑️ | **DONE / closed** 2026-06-13 | Yes | `ShapModel.fit` entry-keyed soft labels (`fuzzy={'P05067': 0.6}`) shipped. |
| 130 | 3 | ☑️ | **DONE / closed** (PR #221 `ea275f6f`) | Yes | `single_logo` accepts `df_parts`+`labels`(+`label_test`,`tmd_len`); frames computed via `AAlogo` getters through the frontend; regression test vs the two-getter path. Cleanup added a `tmd_len`-without-`df_parts` guard + negative test. |
| 131 | 2 | 🔄 | **CONFIRM-FIRST `config.py`**; fits | No | Session-persistent plot style: `options['plot_settings']` applied lazily by plot entry points (or `plot_context`). Unset → byte-identical default (regression test). Needs `config.py` approval. |
| 132 | 2 | 🔄 | **Breaking (deprecation alias)**; fits; **unblocked** | No | Unify label/marker names: `label_test`/`label_ref` canonical; `ShapModel.fit` `label_target_class` → deprecated alias (one `DeprecationWarning`); decide `labels` vs `list_labels` rule. **#74's `deprecated()` helper now exists** (`ut.deprecated`) — wire to it directly. |
| 133 | 3 | 🔄 | **Breaking; needs a new ADR**; fits | No | One `*Plot` return contract (candidate: always `(fig, ax)`; data via trailing `_` attr). Deliverable = ADR + introspection meta-test (+ migration behind a transition). CHANGELOG/`release_notes` machinery now exists (#74). |
| 134 | 2 | ☑️ | **PARTIAL — kept OPEN** (PR #220 `e31c0fd1`) | Partial | **Merged:** `dPULearn`/`TreeModel`/`ShapModel` → `Wrapper`, `AAMut` → `Tool`, + `isinstance` meta-test in `tests/unit/api_tests/`. **Open:** `SeqMut` has no `.run` (exposes `mutate`/`scan`/`suggest`/`eval`) so it was left unbound — the remaining decision is whether to give it a `.run` and inherit `Tool`. `AAWindowSampler` stays a sampler. Bring the `SeqMut.run` call to a review session. |
| 135 | 2 | ☑️ | **DONE / closed** (`5a85193e`, PR #152) | Yes | Implicit contracts surfaced at the docstring surface; `versionadded` directives added package-wide. |
| 157 | 2 | ☑️ | **DONE / closed** 2026-06-13 (`b831b2d6`, PR #166) | Yes | `dPULearn.fit` accepts 1/0 labels (`label_pos`/`label_unl`) + `n_neg` alias; cheat-sheet recipe updated. |
| 158 | 3 | ☑️ | **DONE / closed** 2026-06-13 | Yes | `AAclustPlot.centers`/`medoids` plot directly from a fitted `AAclust` (drop manual transpose + `labels=`). |
| 214 | 3 | 🔄 | Additive; fits; `_cpp_plot.py`; **overlaps #127** | **🔄 IN PR #222** (open, with #127) | Add `sample=`(int/accession)+`df_seq=` to `CPPPlot.profile`/`feature_map`; with `shap_plot=True` resolve (a) the `feat_impact_<name>` column and (b) `*_seq` via `SequenceFeature.get_args_seq` (don't duplicate slicing); auto-resolve `name_test`. Keep `col_imp=`/`**args_seq` path working (back-compat test). **Same two methods as #127 → serialize, one owner.** Edits ShapModel cheat-sheet recipe + ShapModel/`sf_get_args_seq` notebooks. |

### Pro structure plots (new) — `feature_engineering_pro/` (SERIALIZE)
| # | prio | verdict | scope / standards | already-addressed | implementation note |
|---|---|---|---|---|---|
| 119 | 1 | 🔄 | **pro**; oversized but well-specified; CONFIRM-FIRST | No (both halves exist; rendering layer is the gap) | New `CPPStructurePlot.map_structure(df_feat, pdb=...)` → `StructureView` (py3Dmol or mpl fallback). **Reuse, don't reinvent:** map via `get_positions_` + `get_df_pos(value_type="sum")`; load via `load_structure()`; AlphaFold auto-fetch via `StructurePreprocessor.fetch_alphafold`. New `ut` colour constants (impact ramp + pLDDT palette) — **CONFIRM-FIRST `utils.py`**; add `py3Dmol` to pro extra + `__init__.py` stub. Relates ADR-0028. **Blocks #120.** |
| 120 | 2 | 🔄 | **pro**; blocked-by #119 | No | `CPPStructurePlot.interactive(predictor, sequence, pdb=...)` → ipywidgets panel; debounced callback re-runs a user `predictor → df_feat` and repaints the #119 view in place. ipywidgets lazy-imported; add to pro extra (CONFIRM-FIRST). Headless smoke + callback-wiring tests; manual JupyterLab check documented. **Do after #119.** |

### topic:core
| # | prio | verdict | scope / standards | already-addressed | implementation note |
|---|---|---|---|---|---|
| 219 | 2 | ✅ | Additive (mostly); fits; v1.1; **edits `_cpp_plot.py` + `_utils/utils_plot_elements.py`** | No | Dense-plot **row-label overlap test gate** + shrink-font-to-floor-then-grow-height auto-sizing for `feature_map`/`heatmap`/`ranking`/`profile`. Well-specified (validated detector + high-row `df_feat` fixture from `CPPPlot()._df_cat`; KPIs: 0 row-label overlaps at 20/36/55/74 subcats, font never <5pt). Route font scaling through `plot_settings` (maintainer steer), not a bespoke knob. **CONFIRM-FIRST** only if it changes the public `feature_map` signature/default (figsize trigger). **Overlaps PR #222** (same two methods) + the doc/cheat-sheet lane → **serialize behind #222.** Also re-run + re-commit affected notebooks + cheat-sheet figure. |
| 16 | 1 | 🔄 | Fits; overlaps #53 (uncertainty XAI) | No | Bootstrap/variance over CPP features; coordinate with #53 to avoid dup. v1.2 (feeds #60). |
| 21 | 1 | ☑️ | **DONE / closed** (`55528957`, PR #154) | Yes | Prediction-tasks concept page + minimal CPP notebook shipped. Entry point to #35 now exists. |
| 37 | 1 | ☑️ | Fits; **merged, issue kept OPEN for review** | Yes | AAMut/SeqMut finished (`33248426`+`0c090bff`; PRs #112+#117). Was the gate for #57–60 → chain unblocked. |
| 57 | 1 | 🔄 | Fits; **UNBLOCKED (#37 landed)** | No | Top-k residue selection from feature importance; reduced libraries; build on the merged AAMut/SeqMut + ΔCPP engine. First in Lane C. |
| 59 | 1 | 🔄 | Fits; **UNBLOCKED**; after #57 | No | Weighted/Pareto mutation ranking. "No black-box" = keep interpretable. |
| 60 | 2 | 🔄 | Fits; **UNBLOCKED**; after #59 | No | Uncertainty/diversity candidate selection over CPP feature space. |
| 74 | 1 | ☑️ | **DONE / closed** (`29f886e7`, PR #153) | Yes | `CHANGELOG.md` (Keep a Changelog) + `deprecated(reason, version_removed)` decorator shipped (`_utils/decorators.py`, reachable via `ut`). **Unblocks #132/#133** (#157 shipped). |
| 81 | 1 | ☑️ | **DONE / closed** 2026-06-13 | Yes | Single-source HTML+PDF cheat sheet shipped under `docs/_cheatsheet/` (`docs/source/_static/cheat_sheet.html`/`.pdf`); polish commits land directly (e.g. `33cc112b`). |
| 87 | 3 | 🔄 | Optional API (feature_engineering) | No | Named CPP strategy preset; doc-first (#86 shipped as P6), add sugar only if it earns surface. |
| 106 | 1 | 🔄 | Docs **EPIC** | No | Docs-architecture epic; parent of #107/#108/#109. Split into children before executing; coordinate with #35/#80 (overlapping surface — one owner). (#21 concept-page + #81 cheat sheet now closed.) |
| 107 | 2 | 🔄 | Docs; nav + landing routing + API stable/experimental | No | Sphinx toctree/landing + API stable-vs-experimental split (Protein Design → Experimental, cross-ref #37). Child of #106; cheapest/highest-leverage. |
| 108 | 3 | ✅ | Docs; tutorial header box + comparison harness | No | 'You will learn' box across tutorials + wire `tutorial6_comparison_harness`. Child of #106; low-risk additive. |
| 109 | 2 | 🔄 | Docs; new `use_cases/` folder | No | End-to-end biological case studies — distinct from protocols (#35) and tutorials. Child of #106; confirm boundary. |
| 27 | 3 | 🔄 | Fits; overlaps #18/#28 | No | Region abstraction (custom windows); coordinate with the landed output schema. |
| 36 | 3 | 🔄 | Fits but one-liner | No | Needs a concrete scale-selection method spec before coding. |
| 42 | 3 | 🔄 | Oversized; overlaps #40 structure | No | PTM/structure-derived scales touch `_data/` (CONFIRM-FIRST) + DSSP/AlphaFold. Split. |
| 43 | 2 | ☑️ | **DONE / closed** (`94d9b0bc`+`5e380c14`) | Yes | AAclust on protein/CPP-feature matrices + medoid reps; protein-selection plot + `filter_seq` (CD-HIT) comparison + tutorial3a `select_scales`/`select_proteins`. |
| 22 | 2 | 🔄 | Fits; embedding cluster; **partially landed** | Partial (`EmbeddingPreprocessor` #145/ADR-0029) | Embedding **input** path now exists (`fetch_embeddings` + `pool_embeddings`, `[embed]` extra). Live gap = per-*sequence* pooled-vector **fusion with CPP** + joint scoring. Primary of embedding lane. |
| 24 | 3 | ✅ | Small; fits; overlaps #211 | **🔄 IN PR #223** (with #211) | sklearn Pipeline wrapper over CPP features + example. **Coordinate with #211** (same `FunctionTransformer`-over-`feature_matrix` surface) — one owner; both in PR #223. |
| 26 | 3 | 🔄 | Fits; **unblocked** (#18 done) | No | Define ProtXplain-facing API on the landed `df_feat` contract (`LIST_COLS_FEAT`+`sort_cols_feat`). |
| 210 | 3 | 🔄 | Fits (epic); v1.4 | No | **Epic — don't build directly.** Ecosystem integration: consume upstream (`read_fasta`/`EmbeddingPreprocessor`/`StructurePreprocessor`) + competitor descriptors as `X`, expose downstream (sklearn/XAI/design). Memory anchor; spin out children, each with a quantified KPI; **no heavy core deps** (CONFIRM-FIRST per child). Relates #55/#51/#91/#106/#57–60/#53. |
| 211 | 3 | ✅ | Small; additive; **no new public symbol**; v1.4 | **🔄 IN PR #223** (with #24) | Prove sklearn `Pipeline` compat: wrap stateless `sf.feature_matrix(df_feat, df_parts)` in a stock `FunctionTransformer`; integration test (`Pipeline([cpp_feats, RF]).fit/predict`) + **byte-identical** regression vs direct call + leakage-aware recipe notebook (discover `df_feat` once outside CV). Child of #210; **merge with #24**. Tiny/mock fixture so nbmake runs without `torch`/`fair-esm`/`biopython`. |
| 44 | 3 | 🔄 | Research-y; fits loosely | No | Temporal CPP (ΔCPP/Δt). Needs a concrete time-point data contract. |
| 45 | 3 | 🔄 | Fits; reuses `scan_motif` | No | Project motif hits → `df_feat`; overlay on `plot_feature_map`. Use real FIMO p/q-values (ADR-0021). |
| 46 | 3 | 🔄 | Oversized (embedding+structure alignment) | No | Split: CPP-distance alignment in-scope; rest → ProtXplain. |
| 40 | 2 | 🔄 | Oversized; pro; structure cluster | Partial (StructurePreprocessor exists) | Scope to one slice (e.g. DSSP→scales); coordinate with the pending `data_handling_pro` move. |

### topic:data
| # | prio | verdict | scope / standards | already-addressed | implementation note |
|---|---|---|---|---|---|
| 65 | 3 | 🔄 | Pro (biopython); structure cluster | No | `get_msa` misnamed (fetches 1 FASTA). Rename/replace; add real homolog+align backends; relates ADR-0017. |
| 33 | 2 | ✅ | Small; fits; **unblocked** (#18) | No | CSV/parquet/metadata export of `df_feat`; serialize against the canonical `LIST_COLS_FEAT` order. |
| 29 | 2 | ✅ | Small; fits; **unblocked** (#18) | No | z-score/min-max normalization; append normalized columns via `sort_cols_feat`'s tolerant tail. |
| 28 | 2 | 🔄 | Fits; **unblocked** (#66); overlaps #27 | Partial (windowing exists) | Sliding-window CPP on `AAWindowSampler`; coordinate with #27/#18. |
| 23 | 3 | 🔄 | Fits; embedding cluster | No | CPP↔embedding correlation + decorrelation view; pairs with #22. |
| 25 | 3 | 🔄 | Touches `_data/` (CONFIRM-FIRST) | No | Curate benchmark dataset + protocol; needs maintainer sign-off on bundled data. |
| 76 | 3 | 🔄 | Fits; small | No | Explicit entry removal + opt-out flag (today silent). Small data-handling change. |

### topic:performance — SERIALIZE (one owner)
| # | prio | verdict | scope / standards | already-addressed | implementation note |
|---|---|---|---|---|---|
| 19 | 3 | ☑️ | **DONE / closed** — covered by the same-output sweep | Yes | `df_parts`-build optimization landed with the 2026-06 vectorization sweep (#180/#186/#188 + ADR-0032). |
| 62 | 3 | 🔄 | Oversized (CuPy/torch); opt-in only; v1.5 | No | GPU must stay optional, CPU default. Big; the only open perf issue. |

### topic:XAI — mostly ProtXplain scope (🔄)
| # | prio | verdict | scope / standards | already-addressed | implementation note |
|---|---|---|---|---|---|
| 47 | 3 | 🔄 | Pro (shap exists); embedding cluster | Partial (ShapModel pro) | Map attributions → CPP scale space; build on the shipped #63 `shap_plot` bars; coordinate with #22. |
| 48 | 3 | 🔄 | Pro; concept aggregation via AAclust | No | Cluster CPP scales → concepts; in-scope-ish but sizable. |
| 49 | 3 | 🔄 | New deps (FAISS/alibi/dice) → ProtXplain | No | Prototypes/counterfactuals; revisit scope first. |
| 56 | 2 | 🔄 | Oversized (dash/NGLview/PyMOL) → ProtXplain | No | Interactive multi-scale viz not core; split the static parts only. |
| 50 | 3 | 🔄 | New deps (imodels/alibi) → ProtXplain | No | Rule extraction; research-scoped. |
| 51 | 3 | 🔄 | DL stack (captum) → ProtXplain | No | Neural XAI; out of core. |
| 52 | 3 | 🔄 | → ProtXplain | No | Surrogate models; out of core. |
| 53 | 3 | 🔄 | New deps (PyMC) → ProtXplain; overlaps #16 | No | Uncertainty XAI; merge intent with #16. |
| 54 | 3 | 🔄 | Heavy (DoWhy/causal-learn) → ProtXplain | No | Causal XAI; research-scoped. |
| 55 | 3 | 🔄 | Scope split (metrics slice in-core) | No | Fidelity/stability metrics → `aaanalysis/metrics/`; cross-method DL-XAI benchmarking → ProtXplain. |

### topic:other / docs
| # | prio | verdict | scope / standards | already-addressed | implementation note |
|---|---|---|---|---|---|
| 69 | 3 | 🔄 | Docs; **targets don't match real layout** | No | Retarget to real modules; repo uses `.claude/rules/`, not module READMEs — confirm direction first. |
| 75 | 3 | ✅ | Fits; small | No | Route library output through logging, keep `print_out` as a shim (`_utils/utils_output.py` still bare-`print`s). |
| 77 | 3 | 🔄 | Fits; **premise corrected** | No | No `check_cat` validator exists — it's a redundancy-filter knob in `_backend/cpp/_filters/_redundancy_filter.py`; audit retargeted. |
| 79 | 3 | 🔄 | Fits; **premise corrected** | No | CPP already ingests `(L,D)` via `run_num`, so PSSM = a `(L,20)`→`dict_num` glue, not a new pipeline. |
| 88 | 2 | ☑️ | **DONE / closed** (PR #224 `300e9bc0`) | Yes | Audited subprocess (FIMO/BLAST/MMseqs2/CD-HIT/ChainSaw) + network + file parsing; added the missing `get_msa` `timeout=` + an AST timeout-hygiene meta-test; audit recorded in `docs/guides/security_audit_88.md`. **NOT a SECURITY.md** (sharp-edges reject). |
| 89 | 3 | 🔄 | Fits; residue sub-mode ergonomics | No | First-class between-residues/bond-centered features (today implicit via even window + Pattern). Builds on AAWindowSampler. |
| 80 | 2 | ✅ | Docs; gallery; deps=CONFIRM-FIRST | No | nbsphinx thumbnail gallery; reuse the notebook→rst pipeline. Any new docs dep is a pyproject CONFIRM-FIRST decision. |
| 20 | 2 | ☑️ | **DONE / closed** (`5f4177d9`, PR #142 + `8ad0dc0d`) | Yes | `SequenceFeature.get_feature_descriptions` + package-wide `versionadded`. Standardized feature descriptions/terminology shipped. |
| 35 | 2 | ✅ | Docs; **EPIC** (living) | **10/10 protocols shipped + green** | Protocols epic. Open tail: standalone *Scale selection (AAclust)* protocol. Children #91/#93/#80 remain (#20/#21/#81 closed). |

## Defer / Reject appendix (with cited rule)
- **#64 ⏸️** — conservation as a first-class feature is **deferred by ADR-0012** (`defer-comp-seq-cons`);
  blocked on the MSA pipeline (#65) + the pro move. Do not start until ADR-0012 is revisited.
- **#41 / integration+e2e ⏸️** — `tests/integration/` & `tests/e2e/` are **v2-deferred** (`sharp-edges.md`).
- **#62 / #56 / #49–55 🔄→scope** — not rule-violations, but they balloon the dependency surface or
  belong to **ProtXplain** (#26); keep the `pro`/ProtXplain boundary per `pro-core-boundary.md`.
- **#119 / #120 — pro-only**: py3Dmol/ipywidgets land in the `pro` extra (CONFIRM-FIRST
  `pyproject.toml`), never core; matplotlib fallback (#119) adds no dep.
- No issue currently triggers a hard-rule **Reject** (no `AAanalysisError`/SECURITY.md/ruff/mypy asks
  outside the already-triaged #67 umbrella).
