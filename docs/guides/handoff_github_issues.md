# GitHub issues — implementation handoff

_Generated by the `github-issues` skill on **2026-09-17** (previous passes: 2026-09-15, 2026-09-10, 2026-07-14). Regenerate:_
_`python3 .claude/skills/github-issues/scripts/fetch_issues.py`, then re-run the skill._

Audits every open issue against package scope (interpretable, CPP-centered, sequence-based
protein prediction; `pro` extra for heavy deps; semver-strict v1) and the standards in
`CLAUDE.md` + `.claude/rules/sharp-edges.md`. Verdicts: ✅ Ready · 🔄 Revisit
(decision/under-spec/oversized) · ⏸️ Defer-v2 · ❌ Reject (rule conflict) · ☑️ Done/Partial.

> **⚠️ Verification note.** Everything below was verified against **`origin/master`**
> (`git show origin/master:<path>`), not a working tree. A local checkout that is behind will
> not contain `aaanalysis/_provenance.py`, `_schemas.py`, `notebook_examples.yml` or the whole
> v1.1.0 prediction/design tier, and auditing against it produces false "not built" verdicts.
> Re-check `git rev-list --left-right --count master...origin/master` before trusting any
> "already-addressed" cell. **This bit during this refresh:** the main checkout was **118
> commits behind** `origin/master`, and `git log -- <file>` run there reported the handoff's
> own last edit as 2026-09-10 when the true last edit was 2026-09-16. Read from a worktree
> cut from `origin/master`.

> **⚠️ `gh` pagination trap.** `gh issue list --state closed --limit 60` does **not** sort by
> `closedAt`, so filtering its output by close date silently misses recent closures. This pass
> first concluded "zero issues closed", which was wrong — #87 had closed. Always scan with
> `--limit 400` before asserting a closure count.

## What changed since the last refresh (verified 2026-09-17, `origin/master` = `0bf2cb86`)

- **The tracker moved a lot.** **86 open** (was 73) · **148 closed**. The arithmetic reconciles
  exactly: 73 + **14 opened** − **1 closed** = 86.
- **The one closure: #87** (`get_split_kws(strategy=)`), closed 2026-09-16 by PR #528. It is the
  only issue closed in this campaign — every other advanced issue was deliberately left open
  under the standing *shipped code is a first draft* rule.
- **14 new issues**: #545 (non-finite validator, `type:bug`), #546 (bound total peak memory +
  resumable chunked CPP — the remainder spun out of #485), **#548–#555** (`type:docs`, the #455
  protocol children), **#556–#558** (`type:feature`, the #210 ecosystem children), and **#571**
  (gate the docs build).
- **31 PRs merged since this document was authored** (2026-09-15); **20** since the refresh
  commit itself landed (`c9ad4a1a`, PR #536); **17** in the overnight campaign alone
  (since 2026-09-16 18:00Z).
- **Milestones (open issues): v1.3 = 20 · v1.4 = 17 · v2.X = 16 · none = 14 · v1.2 = 10 ·
  v1.5 = 9.** The 14 unmilestoned are exactly the 14 new issues — **milestoning them is the
  cheapest outstanding tracker action.**
- **Label rename: `type:dcos` → `type:docs`** (it was a typo). 38 issues carry the corrected
  label across all states, 11 of them open. `type:dcos` no longer exists — do not reference it.
- **Kanban gap is CLOSED.** Project #1 now holds 155 items (153 issues) and **zero open issues
  are missing** from it. The six previously-missing issues (#276, #365, #429, #455, #509, #510)
  have all been backfilled. Re-verify with
  `gh project item-list 1 --owner breimanntools --format json --limit 400`.

### Four PRs are open and awaiting a merge decision

All four are `MERGEABLE`, all checks green, **none has a review decision** — they are waiting on
the maintainer, not on work.

| PR | branch | what it does | files it locks |
|---|---|---|---|
| **#569** | `doc/contributing-sync` | Ports 220 missing lines back into the root `CONTRIBUTING.rst`; the docs port had drifted *ahead* of the file it mirrors | `CONTRIBUTING.rst`, `docs/source/index/CONTRIBUTING_COPY.rst`, `docs/guides/issue_style_guide.md` |
| **#570** | `doc/455-audit-p2-p3` | Records the P2/P3 rubric audit. **Both already passed; no notebook changed** — the missing thing was the record, not the work | `docs/guides/protocol_rubric_audit.md`, `CHANGELOG.md`, `release_notes.rst` |
| **#572** | `fix/docs-build-errors` | Takes the Sphinx build **27 docutils errors → 0** | `aaanalysis/_schemas.py`, `_struct_preproc.py`, `_seqmut.py`, `docs/source/create_notebooks_docs.py`, `df_schemas.rst`, `use_case1_gamma_secretase.ipynb` |
| **#573** | `fix/545-nonfinite-validator` | Rejects `NaN`/`±inf` in the shared numeric validators, removing three duplicated per-call-site guards | `aaanalysis/_utils/check_type.py`, `_aa_pred.py`, `_reliability_model.py`, `CHANGELOG.md`, 2 test files |

> **#571 should be re-scoped before it is built.** The issue proposes a *ratchet against 24
> errors*, modelled on `pyright_baseline.txt`. **PR #572 takes the count to zero**, so the gate
> should be a plain **zero-error check** — simpler, stricter, and with no baseline file to
> maintain. Merge #572 first, then write #571 against zero.
>
> #572 also corrects the issue's diagnosis: **22 of the 27 errors were one nbconvert bug**, not
> broken docstrings. `parsed-literal` blocks let docutils interpret inline markup inside raw
> program output, so a CPP progress bar (`|....|`) read as a substitution reference and a printed
> `ad_threshold_ = 5.568` read as a link target. `_reliability_model.py` was blamed and is
> untouched. Anyone picking up #571 should not go hunting docstrings.

### What this campaign actually landed

Verified against `git log`, `CHANGELOG.md` and the code — not against PR titles.

- **#455 protocols**: P4/P5/P7/P8 each gained a concept-contrast figure (#562/#563/#564/#566);
  P6/P9/P10 were audited with the audit **recorded** in `docs/guides/protocol_rubric_audit.md`
  (#567); P2/P3 were found **already passing** (record in flight as #570); P10 now uses the
  package learning curve instead of hand-rolling sklearn's (#559).
- **#476 candidate lineage** shipped opt-in (#568): content-hash `candidate_id`, `parent_id`,
  ordered parent-relative `mutations`, `method`, `objective_values`, seed, constraints digest,
  plus `trace_lineage`. No new top-level symbol.
- **#16** gained a bootstrap-CI **renderer** in `CPPPlot.ranking(show_ci=, ci_color=)` (#565) on
  top of the `df_feat` interval columns (#539).
- **A beta-feature overview page** shipped (#547), generated from the code rather than
  hand-maintained.
- **Release notes restructured and condensed** (#561 + follow-ups): the page had lost its title
  underline to the conflict auto-resolver, which promoted every version section to top level and
  blew up the sidebar. *This is the second time that has happened — see the sharp edge.*
- **A real correctness fix rode along:** `CPP.run(n_batches=)` applied the BH FDR correction
  per batch, so `p_val_fdr_bh` depended on how the scale axis happened to be partitioned. It is
  now pooled across batches and matches the single-pass run (#560, under #485).

## Snapshot

- **Open issues: 86.** Verdict split: ✅ Ready **12** · 🔄 Revisit **29** · ⏸️ Defer-v2 **13** ·
  ❌ Reject/route **1** · ☑️ Done-or-Partial **31**.
- **In flight now (open PRs): #569, #570, #572, #573** — all green, all awaiting a merge decision.
- **Recently resolved on master:** #87 only (PR #528). Everything else advanced but stayed open.
- **The 31 ☑️ issues are PARTIAL, not done** — mean **~79%** complete (up from ~57% last pass,
  because this campaign pushed ten of them into the 85–95% band). ☑️ here means
  *Done-or-**Partial***, and in nearly every case it is the second.

> **Correction retained from 2026-09-10 — still the governing rule.** An earlier pass of this
> handoff labelled several issues "shipped — recommend close". That was wrong. **Shipped code is
> a FIRST DRAFT** the maintainer still intends to extend, so these are *partially satisfied
> intents*, not finished work. Closing them would delete the plan for the remainder. Report the
> percentage and the concrete remainder; do **not** recommend closing.

## Issue ↔ PR activity (secondary)

> **Issues on the left** (the spine); their PR(s) on the right — **—** when an issue has no PR.
> `✅` = a PR closed the issue · `↗` = PR(s) advance it (no closing keyword) · `⧗` = PR still open.

| issue | state | PR(s) | what landed / in flight |
|---|---|---|---|
| #87 | ✅ closed | #528 ✅ | `get_split_kws(strategy="compositional"\|"positional")` preset. **The only closure this campaign** |
| #16 | 🔄 open (~70%) | #539 ↗, #565 ↗ | `df_feat` bootstrap CI columns, then the `CPPPlot.ranking(show_ci=)` renderer |
| #26 | 🔄 open (~90%) | #527 ↗ | `df_pred` / `df_rel` / `df_eval_reliability` pinned in `DICT_DF_SCHEMAS` + contract tests |
| #79 | 🔄 open (~90%) | #529 ↗ | `NumericalFeature.from_pssm` feeds `CPP.run_num` |
| #93 | 🔄 open (~95%) | #530 ↗, #559 ↗ | `ModelEvaluator.learning_curve` + plot sibling; P10 now uses it |
| #210 | 🔄 open (~60%) | #535 ↗ | `ecosystem.rst` de-orphaned; its children now exist as #555–#558 |
| #391 | 🔄 open (~95%) | #531 ↗ | `group_cluster(kind="dendrogram", layout="rectangular"\|"circular")` |
| #455 | 🔄 open (~80%) | #519 ↗, #534 ↗, #567 ↗, #559 ↗, **#570 ⧗** | rubric + P2/P3 pass + P6/P9/P10 recorded audit; children #548–#554 carry the per-protocol figures |
| #473 | 🔄 open (~95%) | #526 ↗, #541 ↗ | `ad_status` / `ad_threshold_` / `ad_method_` / `ad_nearest_train`, then `predict_candidates` |
| #474 | 🔄 open (~90%) | #538 ↗, #543 ↗ | `AAPred.eval_selective` risk–coverage + AURC; acceptance criteria asserted verbatim |
| #475 | 🔄 open (~85%) | #537 ↗, #544 ↗ | `DesignConstraints` + all three consumers; notebooks show one shared instance |
| #476 | 🔄 open (~90%) | #568 ↗ | opt-in candidate-lineage record + `trace_lineage` |
| #480 | 🔄 open (~90%) | #525 ↗ | `eval(use_calibrated=, add_metrics=)` with Brier + ECE; diagram annotates them |
| #485 | 🔄 open (~70%) | #532 ↗, #560 ↗ | chunking contract tests + corrected memory claim; **pooled BH FDR across batches** |
| #509 | 🔄 open (~90%) | #533 ↗ | `predict_samples` parity anchor, ≤10-line KPI, pipe param-coverage gate, Golden Pipelines page |
| #510 | 🔄 open (~85%) | #511 ↗, #507 ↗, #524 ↗ | all seven consistency bullets landed; only the banner removal remains |
| #545 | 🔄 open (0% landed) | **#573 ⧗** | non-finite rejection in `check_number_range` / `check_number_val` |
| #548 · #549 · #550 · #551 | 🔄 open (~90%) | #562 · #563 · #564 · #566 ↗ | one concept-contrast figure each for P4 / P5 / P7 / P8 |
| #552 · #553 · #554 | 🔄 open (~95%) | #567 ↗ | P10 / P6 / P9 audited, recorded, gaps closed |
| #571 | 🔄 open (0% landed) | **#572 ⧗** | #572 clears the 27 errors; the *gate* itself is unbuilt and maintainer-gated |
| #37 · #40 · #22 · #36 · #47 · #53 · #56 · #109 · #488 | 🔄 open (partial) | — (pre-campaign work) | see the per-issue audit; unchanged this pass |
| all other open issues | 🔄 open | — | no PR yet — see the per-issue audit |

**Program / tooling PRs with no backing issue (this campaign):** #547 (beta-feature overview
page), #561 (release-notes title + duplicated section), #540 (notebook section underlines after
the API linkify pass), #542 (structure feature-map aspect ratio), #536 (the previous handoff
refresh), **#569 ⧗** (CONTRIBUTING sync). Earlier: #523, #521/#502, #520/#503, #519, #518 +
#512–#517, #511, #507/#506/#504, #463.

---

## ▶ What to work on next — sequenced, with effort

### The effort scale (calibrated on this repo, not generic)

Effort here is dominated by the **fixed overhead every new public symbol pays**, not by the
algorithm. A new symbol must clear ~15 gates in `tests/unit/api_tests/` (abbreviation registry,
docstring contracts, plot-return contract, param coverage, backend-import hygiene), get a
docstring in house style, an example notebook demonstrating **every** public parameter (a
permanent zero-gap CI gate), and ~30 tests per method. Sizing therefore keys off *what kind of
surface* the change adds:

| Size | Means | Measured anchor |
|---|---|---|
| **S** | ≤ ½ day. No new public symbol; ≤ 2 files; extends existing tests | `ad_nearest_train`: `kneighbors()` already computes the indices |
| **M** | 1–2 days. New *method* on an existing class, or a cross-file rename pass. Docstring + notebook params + ~30 tests | `AAPred.predict_oof` (#398), `eval_selective` (#474) |
| **L** | 3–5 days. New public *class* or module: logic + `Plot` sibling + backend subpackage + registry entries + CONFIRM-FIRST `__init__.py` | `DesignConstraints` (#475), `ReliabilityModel` |
| **XL** | > 1 week, or blocked until a decision record exists | #276, #483, #546, the omics track |

### Implementation order (do top-down; respect blockers + in-flight PRs)

1. **Merge the four open PRs** (maintainer decision, ~0 work) — **#572 first**, because #571's
   whole design depends on whether the baseline is 24 or 0. Then #573, #570, #569. Until these
   merge, four lanes are occupied. — *lane: none, it is a decision*
2. **Milestone the 14 unmilestoned issues** (≈30 min, zero code). #545/#546 → v1.2/v1.5;
   #548–#555 → v1.2 under #455; #556–#558 → v1.4 under #210; #571 → v1.2. The board currently
   shows 14 issues with no release attached, which hides the real v1.2 load. — *lane: none*
3. **#510 — remove the experimental banners** (S, the last checkbox). All seven consistency
   bullets shipped in #524. The banners are the *gate*: once removed, the tier is under strict
   semver. Decide deliberately — this is the single highest-consequence remaining action in the
   v1.2 milestone. — *lane A*
4. **#571 — the docs-build gate** (S, but CONFIRM-FIRST). After #572, write it as a zero-error
   check. — *blocked on maintainer, see below*
5. **#455 — the protocol execution gate** (S). Verified: `grep -rn "protocols" .github/workflows/`
   returns **nothing**, so all ten protocols still have no execution gate. Everything else in
   #455 is done. — *blocked on maintainer (CONFIRM-FIRST), see below*
6. **#16 — the `abs_auc` interval renderer** (S). The `df_feat` columns and the `mean_dif`
   whiskers ship; `abs_auc_ci_low/high` are computed and **rendered nowhere**. — *lane A*
7. **#475 — soft constraints** (M). — *blocked on a design decision, see below*
8. **#478 → #479** (M–L each), then **#481** (M). Lane A continues. — *lane A*
9. **#27** (L, primary of the CPP lane) and **#28** (M, cheapest as an `ap.` verb). — *lane B*
10. **#33, #25, #23, #52, #555** — independent, pick by appetite. — *lanes D/E/F*

### Parallel lanes (safe to run in separate sessions)

- **Lane A — reliability / eval core** (`prediction/_reliability_model.py`, `_aa_pred.py`,
  `_backend/reliability/`, `_backend/aa_pred/`): **SERIALIZE** — #510 → #16 → #478 → #479 →
  #481 → #482. Also #429, #495, #522 land here.
  **⚠️ Occupied right now by open PR #573** (`check_type.py`, `_aa_pred.py`,
  `_reliability_model.py`). Do not start Lane A until #573 merges.
- **Lane B — CPP parts / feature-id plumbing** (`feature_engineering/_backend/cpp/**`,
  `_sequence_feature.py`): **SERIALIZE** — #27 (primary, rewrites feature-id construction) →
  #89 → #28 → #45.
- **Lane C — CPP memory / perf kernels**: **SERIALIZE** — #485 → **#546** → #263 → #492 → #493.
  #546 is now the live head: it carries the resumability and true peak-memory bound that #485
  could not deliver. Every member is output-affecting and needs its own tolerance tier.
- **Lane D — design stack** (`protein_engineering/`): **SERIALIZE** — #475 (soft constraints) →
  #522 → #60. #476 has left this lane (shipped ~90%).
- **Lane E — AAPredPlot dispatch** (`_aa_pred_plot.py` `LIST_*_KINDS`): **SERIALIZE** —
  #392 → #393. #391 is done (~95%), so the lane head is free.
- **Lane F — omics adapters**: **SERIALIZE behind one go/no-go** — #273 → #490 → #489 → #491.
- **Lane G — docs / protocols / notebooks**: #455's gate → #109 → #555 → #548–#554 residue.
  **⚠️ Occupied right now by open PRs #569, #570 and #572**, which between them hold
  `CONTRIBUTING.rst`, `protocol_rubric_audit.md`, `create_notebooks_docs.py`, `df_schemas.rst`
  and `use_case1_gamma_secretase.ipynb`.
- **Genuinely independent:** #79-residue (`_numerical_feature.py`), #55 (`metrics/`),
  #33 (`data_handling/`), #25 (docs), #571 (`.github/`).

### Overlap clusters — serialize, do not develop in parallel

| cluster | issues | primary | why they collide |
|---|---|---|---|
| **Reliability / eval surface** | #510, #16, #473, #474, #480, #478, #479, #481, #482, #429, #495, #522 | **#510** | All edit `_reliability_model.py` / `_aa_pred.py` or the frames they return. #510 renames columns the others add rows and columns to. **Plus open PR #573 holds three of these files right now.** |
| **CPP memory / chunking** | #485, #546, #263, #492, #493 | **#485 → #546** | One feature split across five issues, all in the CPP feature loop. #546 is #485's own spun-out remainder; developing them apart guarantees a conflict. |
| **Design stack** | #475, #522, #476, #60, #37 | **#475** | `DesignConstraints` is the shared object; #522's reliability layer and #476's constraints digest both read it. |
| **Protocols / docs build** | #455, #548–#555, #571, #109 | **#455** | Nine notebooks plus the exporter. **Open PRs #570 and #572 hold `protocol_rubric_audit.md` and `create_notebooks_docs.py`.** |
| **Docs source-of-truth files** | #571 + PRs #569/#572 | — | `CHANGELOG.md` and `release_notes.rst` are edited by **three of the four open PRs** (#569, #570, #573). These will conflict on merge — **merge them one at a time**, not as a batch. |
| **Ecosystem children** | #210, #555, #556, #557, #558 | **#210** | #556/#557 both change how external numeric data enters the feature path; #557 is explicitly a *decision* about #556's shape. |
| **Cross-cutting merge contention (not code overlap)** | #478, #429, #479, #481, #52, #556 | — | Each adds a public symbol → all edit `aaanalysis/__init__.py`, `__all__` and the abbreviation registry test. Logically independent, textually guaranteed to conflict — **merge one at a time.** |

---

## 🚧 BLOCKED — what cannot start, and on what

### Blocked on a maintainer decision (no amount of engineering unblocks these)

| # | what is blocked | why it needs the maintainer |
|---|---|---|
| **#455** | The protocol-execution-in-CI gate | Edits `.github/workflows/*`, a **CONFIRM-FIRST** surface. Everything else in #455 is done; this one checkbox is the whole remainder. Adding `protocols/` to the nbmake gate also lengthens every PR's critical path — a cost decision, not a technical one. |
| **#571** | The workflow wiring | Same CONFIRM-FIRST surface; the issue says so itself. **Also needs a scope call:** #572 makes the count zero, so decide **zero-error gate vs the ratchet-against-24** the issue text proposes. Recommend the zero-error gate. |
| **#475** | Soft constraints (penalty term surfaced to the optimizer) | A design decision, not a gap. Hard rejection shipped; the issue also asks for soft constraints, but where the penalty enters — inside `DesignConstraints`, or as a `SeqOpt` objective term — changes the public shape of both. Decide before coding. |
| **#16** | The remaining scope | Two open questions: (a) whether `abs_auc_ci_low/high` get a renderer of their own or stay data-only, and (b) whether **alternative uncertainty methods** (variance, Bayesian approximations) are in scope at all, or whether bootstrap is the final answer. The issue's task list still names Bayesian; nothing has evaluated it. As a **prio:1** issue this distorts the tracker until resolved. |
| **#522** | Reliability-aware optimisation (DyRAMO-style) | Needs the #475 soft-constraint decision first (it *is* a penalty consumer), and a second call on whether the adaptive-threshold strategy belongs in the package or downstream. |
| **#510** | Removing the experimental banners | Not a coding task: removing them commits the whole prediction/design tier to strict semver. A release-policy decision. |
| **#497** | Whether to close or carve out | The auditors split (❌ close vs ⏸️ park). Maintainer call. |
| **omics track** (#273, #489, #490, #491) | One go/no-go for all four | A dependency-surface decision (anndata/scverse), not four separate issues. |
| **#429** | `[Potential]` banner | Needs a go/no-go, and its premise is false (see the audit row). |

### Blocked on other unfinished work

| # | blocked by | note |
|---|---|---|
| **#571** | **PR #572** | Build the gate against the post-#572 count, not the pre-#572 one. |
| **Lane A (all of it)** | **PR #573** | #573 holds `check_type.py`, `_aa_pred.py` and `_reliability_model.py`. Starting #510 or #16 now guarantees a conflict. |
| **Lane G (docs)** | **PRs #569, #570, #572** | Three docs PRs hold `CONTRIBUTING.rst`, `protocol_rubric_audit.md`, `create_notebooks_docs.py`, `df_schemas.rst`. |
| **#546** | #485 | It is #485's spun-out remainder; the chunking contract must settle first. |
| **#89** | #27 | Needs #27's region abstraction before a `parts="bond"` preset means anything. |
| **#64** | #65 **and** a superseded decision | Contradicts the standing decision to neither test nor integrate the orphaned conservation module. |
| **#498** | #483 | Not startable before a coordinate map exists. |
| **#484** | #22, #23, #40, #483 | Four unbuilt dependencies. |
| **#482** | #61 | `AAPred` is binary-only by construction. |
| **#556 / #557 / #558** | #210 | Children of a tracker whose own KPI is *having children*. #557 is a decision gating #556. |
| **#495** | #481 | Near-duplicate; fold in rather than build twice. |
| **#263** | CI has no GPU runner | The issue's own ≥5× KPI is unverifiable today. |

---

## Per-issue audit

### Prediction / reliability / evaluation

| # | prio · ms | verdict | PR | scope / standards | already-addressed (verified) | implementation note (complements the issue) |
|---|---|---|---|---|---|---|
| #510 | 2 · v1.2 | ☑️ **85%** | #524 ↗ | Fits; the break window is open and closing | All seven bullets shipped: `ci` fraction, `ad_knn`, `n_samples`, `SeqOpt` default `mode`, `mcc`, `COLS_EVAL_RELIABILITY`, `score_std` docs | Remainder is **one checkbox: remove the `.. warning:: Experimental` banners**, plus the deferred `eval` summary-row shape decision. Do this *before* #478/#479 add more surface |
| #473 | 2 · v1.2 | ☑️ **95%** | #526 ↗ #541 ↗ | Correctly refuses a new class | `ad_status`, `ad_threshold_`, `ad_method_`, `ad_nearest_train`, `predict_candidates` all ship | Remainder is only the deliberately-split `[pro]` sequence-identity AD method — a declared **non-goal** in the body. Re-scope to that or fold into a new pro issue |
| #474 | 3 · v1.2 | ☑️ **90%** | #538 ↗ #543 ↗ | Measurement only — clean on the border | `eval_selective` + `score_aurc`; acceptance criteria asserted verbatim | Remainder: the issue wants the confidence source pluggable **including applicability-domain status**; `confidence=` takes an array but nothing documents the AD hand-off. One notebook cell + a doc line closes it |
| #480 | 2 · v1.3 | ☑️ **90%** | #525 ↗ | Body correctly excludes what shipped | `use_calibrated=`, `add_metrics=`, Brier, ECE, diagram annotation | **Re-milestone v1.3 → v1.2**: it shipped in the v1.2 cycle. Verify the `.. versionadded::` sweep and the two notebooks' param coverage, then re-scope |
| #16 | **1** · v1.2 | ☑️ **70%** | #539 ↗ #565 ↗ | 2026-06 stub; the Bayesian arm was never evaluated | `df_feat` gains `abs_auc_ci_*` + `mean_dif_ci_*`; `ranking(show_ci=)` draws **`mean_dif` only** | Two gaps: the `abs_auc` interval is computed and rendered nowhere, and "evaluate methods (bootstrap/variance/Bayesian)" has no recorded decision. **Blocked on a scope call** — see BLOCKED |
| #509 | 3 · v1.2 | ☑️ **90%** | #533 ↗ | Tests + docs only | Parity anchor, ≤10-line KPI parsed from the page, pipe param-coverage at zero gaps, Golden Pipelines page | Remainder: the `find_features` parity decision was **recorded in the docstring** (`"fast"` only) rather than anchored. Confirm that satisfies the requirement or add the pinned-config anchor |
| #93 | 3 · v1.3 | ☑️ **95%** | #530 ↗ #559 ↗ | Core, no dep | `ModelEvaluator.learning_curve` + plot sibling; reproduces `run` at size 1.0; P10 uses it | Essentially done. Confirm the ≥4-sizes and byte-identical-`random_state` KPIs are asserted, then re-milestone or close on the maintainer's call |
| #545 | 2 · none | ✅ | **#573 ⧗** | Core; tightening a guard is not a semver break | Nothing on master yet | PR #573 found the hole is **larger than the issue**: `+inf` also passed on any parameter with no `max_val`. Merge it; then the issue's "remove duplicated call-site checks" requirement is satisfied too |
| #478 | 2 · v1.3 | ✅ | — | Pure sklearn | Nothing | Thin `groups`-bound wrappers. The real justification, missing from the issue: `AAPred.eval` calls `cross_val_*` **without `groups=`**, so a stock sklearn group splitter silently cannot receive them |
| #479 | 2 · v1.3 | ✅ | — | Severity stays human-readable | `pipe/_obtain_samples.py::_window_identity` is reusable prior art | Promote the pipe-private identity helpers to a shared `_backend`/`_utils` **in the same PR** — a prediction module may not import them (cross-package backend rule) |
| #481 | 2 · v1.3 | 🔄 | — | Fits | `SequenceFeatureTransformer` **is** the frozen pipeline | Decide what "a fitted pipeline" is (duck-type `predict`/`transform`); reuse `ood_score` as the shift summary rather than inventing a metric. Fold #495 in here |
| #482 | 3 · v1.3 | 🔄 | — | Re-opens a hard contract | `AAPred` is binary-only by construction (`check_binary_labels` gates fit/eval/predict_oof) | Blocked-by #61. Its `mcc` bullet is now **stale** — #510 shipped `mcc`. Delete that bullet. Ask whether regression belongs in `AAPred` at all |
| #429 | 3 · v1.4 | 🔄 | — | `[Potential]` banner — needs go/no-go | Nothing | **The issue's "no changes to `eval`" claim is false:** `cross_val_predict` requires test folds to partition all samples, so a masking splitter cannot work with `cv_pooled` unmodified |
| #522 | 2 · v1.2 | 🔄 | — | Fits; reward-hacking KPI well-formed | Nothing; `predict_candidates` (#473) now supplies the missing glue | The glue it was waiting for **exists now**. Still blocked on the #475 soft-constraint decision. Add one `reliability_kws` dict (`run` already has 22 kw-only params) |
| #495 | 3 · v2.X | 🔄 | — | **Near-duplicate of #481** | Per-sample side ships as `ood_score` | Fold into #481. Pick a metric (PSI/KS/MMD) before coding |
| #276 | 3 · v1.3 | 🔄 | — | Oversized; a decision record already deferred the engine here | `ModelEvaluator` + `sft` + `find_features(selection_scope="fold")` cover the parts | Two hard blockers: a new boosted-tree extra (CONFIRM-FIRST) and grids that exist only in a paper supplement. Split core/extra |
| #60 | 2 · v1.4 | 🔄 | — | Stub; BoTorch/modAL would balloon deps | Nothing; building blocks exist | Stateless `select_candidates(...)`, not a stateful AL loop — campaign state is downstream |

### Design stack (protein engineering)

| # | prio · ms | verdict | PR | scope / standards | already-addressed (verified) | implementation note |
|---|---|---|---|---|---|---|
| #475 | 2 · v1.2 | ☑️ **85%** | #537 ↗ #544 ↗ | Explicitly not Pydantic — rule-clean | `DesignConstraints` ships public (`aa.DesignConstraints`, abbrev `dc`) with `check()`, `as_predicate()`, `to_dict`/`from_dict`, all three consumers, 4 example notebooks | Remainder is **soft constraints** — the issue asks for hard *and* soft; only hard rejection plus a SeqOpt sequence-level penalty shipped. **Blocked on a design decision** — see BLOCKED |
| #476 | 3 · v1.2 | ☑️ **90%** | #568 ↗ | Plain dict, mirrors the thin provenance record; `sha256`, never `hash()` | `SeqMut.combine(lineage=)` / `SeqOpt.run(lineage=)` → `lineage_` + `candidate_id` column; `trace_lineage` walks a chain | Two gaps: the changelog names only `SeqMut` and `SeqOpt`, but the issue also names **`AAMut`**; and the 3-generation export→reimport KPI should be asserted end to end, not just the round-trip |
| #37 | **1** · v1.2 | ☑️ **75%** | — | — | All six design classes exported, 20 executed notebooks | Unchecked boxes are out of scope by decision or spun out (#475, #522, #476). **Split**: the figure pass (the maintainer's own hold) stays v1.2; AAclust integration + cross-dataset eval → v1.3 |

### CPP core, features, performance

| # | prio · ms | verdict | PR | scope / standards | already-addressed (verified) | implementation note |
|---|---|---|---|---|---|---|
| #485 | 2 · v1.5 | ☑️ **70%** | #532 ↗ #560 ↗ | Headline largely shipped | `n_batches` / `n_sample_batches` ship; **BH FDR now pooled across scale batches** (a real statistical defect, fixed); memory claim corrected | The resumability and total-peak-memory half is **now #546** — treat #485 as the contract-and-correctness issue and #546 as the out-of-core one. Its original byte-identical KPI stays unachievable on the sample axis (documented ULP drift) |
| #546 | 3 · none | ✅ | — | Core; Lane C head | Nothing | Spun out of #485 with a clean scope. **XL**: a checkpoint format is a durable on-disk contract — write the decision record before any code. Milestone it (v1.5 alongside #485) |
| #79 | 3 · v1.5 | ☑️ **90%** | #529 ↗ | Core, additive, no dep | `NumericalFeature.from_pssm` + `_backend/num_feat/read_pssm.py` | Confirm the PSSM→`dict_num` round-trip is demonstrated in the example notebook with every public param. **Re-milestone v1.5 → v1.2** |
| #27 | 3 · v1.3 | ✅ | — | Core, no deps, byte-identical default | `LIST_PARTS` still TMD-only | **Primary of Lane B.** Pin a charset validator: a `-` in a region name breaks `PART-SPLIT-SCALE` parsing everywhere |
| #28 | 2 · v1.3 | ✅ | — | New public symbol → CONFIRM-FIRST | Not built; every piece exists | Cheapest home is an `ap.` verb in `aaanalysis/pipe/`, **not** a new top-level class — the issue never considers this and it halves the cost |
| #23 | 3 · v1.4 | ✅ | — | Core; embeddings user-supplied | Every primitive ships | Correlate against `build_scales` pseudo-scales, not raw dims — only that gives the per-category report. This is #22's unbuilt comparison half verbatim |
| #36 | 3 · v1.3 | ☑️ **55%** | — | `_data/` write is CONFIRM-FIRST **and** semver-risky | Machinery all ships (`pre_select_scales`, `select_scales`, …) | Re-scope to "reproduce top60 as a seeded notebook + regression test"; regenerating bundled `top60` silently changes every user's result |
| #22 | 2 · v1.3 | ☑️ **65%** | — | Core | Sections 1–2 + 4 ship; tutorial 3e teaches the fusion path | Its only live section **is** #23 verbatim. Close in favour of #23 *or* re-scope to the pooled-vector fusion helper + pseudo-category → AAontology mapping |
| #40 | 2 · v1.3 | ☑️ **60%** | — | Duplicate umbrella over #365 | ~70% delivered (DSSP/pLDDT/domains/AF fetch/structural scales/`CPPStructurePlot`) | Migrate the 3D-neighbourhood remainder to #365 and the similarity question to #484. Conformational ensembles need a decision record: `dict_num` has no conformation axis |
| #365 | 2 · v1.4 | 🔄 | — | Benchmark acquisition is a research program | Ideas 2–3 largely built via `StructurePreprocessor` / `EmbeddingPreprocessor` → `run_num` | Narrow to **Idea 1 only** (spatial-patch pseudo-sequence). Patch *ordering* decides whether `Pattern` splits mean anything |
| #89 | 3 · v1.3 | 🔄 | — | Sibling of #27, which excludes its naming | Manual escape hatch exists | Wait for #27's region abstraction, then a `parts="bond"` preset. Recipe before wrapper |
| #45 | 3 · v1.3 | 🔄 | — | `scan_motif` is pro, the CPP side is core — split-brain gating the issue ignores | ~60% remains (no motif→`df_feat` projection) | A motif id is not `PART-SPLIT-SCALE` → use a sibling `df_motif` frame + overlay, not new `df_feat` rows |
| #46 | 3 · v1.5 | 🔄 | — | In-core slice dep-free | Nothing | Keep CPP-distance-to-class-mean; reuse `ReliabilityModel`'s distance machinery |
| #44 | 3 · v2.X | 🔄 | — | Core slice dep-free | Nothing; `CPP.run` is two-group only | Ordered-groups loop over `CPP.run`. Write the decision record first |
| #29 | 2 · v1.3 | 🔄 | — | Self-contradicting | `SequenceFeatureTransformer` already makes the leak-safe route exist | A *stateless* `normalize=` kwarg structurally cannot satisfy the no-leakage requirement. Document the sklearn recipe and close, or ship with an explicit warning |
| #64 | 3 · v1.4 | 🔄 | — | **Contradicts a documented decision** to neither test nor integrate the orphaned conservation module | `_comp_seq_cons.py` exists but is deliberately unexported | Cannot proceed without explicitly superseding that decision; hard-blocked by #65 |
| #493 | 3 · v1.5 | 🔄 | — | Keying now feasible | `@lru_cache` read layer + a df_scales-hash cache exist | `joblib` is already a dep. Two features in one issue — ship the keyed cache, drop checkpoints to #546 |
| #65 | 3 · v1.5 | ⏸️ | — | Balloons the tooling surface past the single-binary FIMO precedent | `get_msa` exists inside the unexported module and fetches one FASTA, not an alignment | Reduce to accepting a user-supplied MSA; rename the misnamed helper (free — never exported) |
| #42 | 3 · v1.5 | ⏸️ | — | Research program + `_data/` rewrite | Partially mechanized (`extend_alphabet`, structural pseudo-scales) | Only shippable slice is a thin `AAclust.fit` + `build_scales` wrapper — file separately |
| #263 | 3 · v1.5 | ⏸️ | — | Self-blocked: the ≥5× KPI needs a GPU runner; no workflow has one | CPU half ships (`n_jobs`) | Output-affecting → tolerance tier + frozen anchor + a new extra. Don't start |
| #492 | 3 · v1.5 | ⏸️ | — | **Sparse one-hot is on the rejected-optimization list** | Zero sparse handling — and **no CPP input is sparse** (`df_parts` strings, `df_scales` dense 20×N) | Solution to a data path the package doesn't have. Revisit only if the omics track lands |

### XAI + AAPredPlot visual layer

| # | prio · ms | verdict | PR | scope / standards | already-addressed (verified) | implementation note |
|---|---|---|---|---|---|---|
| #391 | 3 · v1.4 | ☑️ **95%** | #531 ↗ | Core, no dep | `LIST_CLUSTER_KINDS = ["clustermap", "dendrogram"]`; linkage now computed explicitly with SciPy and passed to seaborn, so both kinds share a topology | The "identical topology" KPI is genuinely satisfied (the trap was avoided). Residue: with `fastcluster` installed, exact ties between equidistant merges may break differently — document or assert loosely |
| #52 | 3 · v2.X | ✅ | — | **Zero new deps** (sklearn tree/Lasso on parent predictions) | Nothing | New `_surrogate_explainer.py` + `*Plot` mirror + backend + hygiene-test owner entry. Decision-boundary bullet must return `FigAxResult`, never call `plt.show()` |
| #56 | 2 · v1.4 | ☑️ **80%** | — | Core slice fits; dashboards routed downstream | **Its own KPI is met** by `feature_map`/`heatmap`/`profile`/`predict_sample` + `CPPStructurePlot` | Keep only as the epic wrapping #392/#393 (#391 is done). Two unowned gaps: a feature-level `CPPPlot.volcano`, and cross-protein side-by-side comparison |
| #47 | 3 · v2.X | ☑️ **45%** | — | Thinnest body in the cluster | `ShapModel.add_feat_impact`, `TreeModel.add_feat_importance`, `ap.explain_features` all ship | Residual = PDP/ICE/ALE/LIME (new pro deps) + PoSHAP + cross-model consistency. Rewrite the body to those |
| #53 | 3 · v2.X | ☑️ **70%** | — | Now largely a duplicate | `ReliabilityModel`, `ShapModel` `feat_impact_std`, `selection_frequency` all ship | Genuinely unbuilt: "SHAP variance **across models**" — `fit()` averages over models before storing. Fold the rest into #16; do not schedule beside it |
| #48 | 3 · v2.X | 🔄 | — | Core slice fits | The concept library largely **is** AAontology (`category`/`subcategory`) | Actionable slice = a ranked concept-importance table (`df_feat` groupby). Don't open a class for a groupby |
| #49 | 3 · v2.X | 🔄 | — | Core slice sklearn-only | `AAclust.comp_medoids` ships; `ReliabilityModel` already fits a k-NN reference | Reuse that reference rather than a second NN index. Class-vs-methods decision first |
| #55 | 3 · v2.X | 🔄 | — | `comp_*` funcs fit the metrics family exactly | Nothing | Two blocking signature questions: what "the explanation" is on input, and what the perturbation is. Then one PR |
| #392 | 3 · v1.4 | 🔄 | — | **Mis-assigns t-SNE to pro** (sklearn already required); only UMAP is new | PCA-scatter machinery exists in `aaclust`/`dPULearnPlot` | Obvious reuse is a **forbidden cross-package backend import** — re-implement locally or promote a shared helper. Lane E head now that #391 is done |
| #393 | 3 · v1.4 | 🔄 | — | Needs `networkx` (CONFIRM-FIRST) | Same corr matrix the clustermap builds | KPI "identical node coordinates" is fragile across versions — assert edge set/degree instead. Or draw with `LineCollection` and skip the dep |
| #558 | 3 · none | 🔄 | — | Wrapping external XAI tooling balloons the dep surface — the same test #50/#51 fail | Nothing | Child of #210. Decide **which** tooling and under which extra before this is actionable; otherwise it is #47's residual restated. Milestone it |
| #50 · #51 · #54 · #265 | 3 · v2.X | ⏸️ | — | **No core slice**: imodels/alibi · captum + DL runtime · DoWhy/EconML/causal-learn/gCastle · gplearn/pysr | #54's own "smallest validatable slice" already ships as `SeqMut` `delta_pred` | All four need the same four-file pro scaffolding — one dep-budget decision, then serialize. #265 is a *method* of #52's class, not a class |
| #498 | 3 · v2.X | ⏸️ | — | Tip of a 4-deep chain of unbuilt issues | Nothing | Not startable before #483. Undefined: what agreement *means* between a signed SHAP value and a conservation score |

### Docs, protocols, use cases

| # | prio · ms | verdict | PR | scope / standards | already-addressed (verified) | implementation note |
|---|---|---|---|---|---|---|
| #455 | 2 · v1.2 | ☑️ **80%** | #519 #534 #567 #559 ↗ · **#570 ⧗** | Docs + CONFIRM-FIRST workflow | Rubric shipped; P2/P3 pass; P4/P5/P7/P8 figures; P6/P9/P10 audited and recorded; 10 protocols exist | **Verified: `grep -rn "protocols" .github/workflows/` returns nothing** — the execution gate is the entire remainder and is maintainer-gated. Second, smaller gap: the audit record covers P6/P9/P10 (and P2/P3 via #570) but **not P4/P5/P7/P8**, which got figures without a recorded audit |
| #548 · #549 · #550 · #551 | 3 · none | ☑️ **90%** | #562 · #563 · #564 · #566 ↗ | Docs-only, rubric-driven | One concept-contrast figure each: P4 three prediction levels on one protein; P5 sequence vs numerical arms; P7 what survived reduction and why; P8 measured feature-selection leakage | Each delivered the figure but **not a recorded rubric audit** (unlike #552–#554). Closing the set properly means one audit entry per protocol in `protocol_rubric_audit.md`. **Milestone them under v1.2 with #455** |
| #552 · #553 · #554 | 3 · none | ☑️ **95%** | #567 ↗ | Docs-only | P10/P6/P9 audited, recorded, **exit verdict PASS on all four rubric sections**; three stale cross-references fixed; P6's false "there is no `strategy=` switch" claim removed | Effectively complete. The one deliberate carve-out: the three gallery thumbnails were **not** regenerated (font-rasterization drift would make 3 of 10 tiles inconsistent). If thumbnails are ever refreshed, refresh **all ten on one machine** |
| #555 | 3 · none | ✅ | — | Docs-only; no new dep | The upstream bridges it documents already ship | Cheapest #210 child by far. One notebook per existing adapter; no library change. Milestone it |
| #571 | 2 · none | ✅ | **#572 ⧗** | CI tooling, no new dep; **CONFIRM-FIRST** (`.github/workflows/*`) | Nothing built; #572 clears the errors it counts | **Re-scope to a zero-error gate** — #572 takes 27 → 0, so the proposed ratchet-against-24 is obsolete. Sphinx exits 0 with errors present, so the count must be parsed from the log. Script → `.github/scripts/` (`dev_scripts/` is gitignored) |
| #109 | 2 · v1.4 | ☑️ **35%** | — | Docs-only | Infrastructure + flagship γ-secretase case study done | Pure content now. **Split into 3 children by prediction level** — a 7-notebook issue never closes. Note #572 fixes a blockquote in `use_case1` that was suppressing a whole paragraph on RTD |
| #25 | 3 · v1.3 | ✅ | — | Docs-layer, no public API | Not addressed; its cited path is stale | Reuse `_data/benchmarks/Overview.tsv` (15 sets, three levels) + `comp_auc_adjusted` + `AAPred.eval(cv=, baseline=)`. **Land before #488** — #25 is the protocol, #488 its runner |

### Data, ecosystem, omics, packaging, v2

| # | prio · ms | verdict | PR | scope / standards | already-addressed (verified) | implementation note |
|---|---|---|---|---|---|---|
| #26 | 3 · v1.3 | ☑️ **90%** | #527 ↗ | Docs + contract test | `DICT_DF_SCHEMAS` now documents `df_pred` (all three levels), `df_rel`, `df_eval_reliability`, guarded by literal column/order/dtype tests | The live-drift risk this issue named is now closed. Remainder: `df_eval_selective` and the design-tier frames are still unpinned. **Do not** add error taxonomies here |
| #210 | 3 · v1.4 | ☑️ **60%** | #535 ↗ | Memory anchor / tracker | `ecosystem.rst` de-orphaned and reachable from the sidebar; **its KPI was "have children" and #555–#558 now exist** | Substance rose from 40% to 60% purely by acquiring children. Next: milestone all four children, then #555 (cheapest) first. Strike the sklearn bullet, superseded by `SequenceFeatureTransformer` |
| #556 | 3 · none | 🔄 | — | Core; ingesting an external descriptor matrix overlaps `CPP.run_num`'s existing contract | `run_num` + `dict_num` already accept arbitrary per-residue numeric data | The real question is whether this is a *loader* or just documentation of `run_num`. Decide that before coding, and decide it **together with #557** |
| #557 | 3 · none | 🔄 | — | Literally a decision issue ("Decide whether…") | `extend_alphabet` + `build_scales` already cover part of it | Resolve this **first**; it gates #556's shape. A decision record, not code |
| #33 | 2 · v1.3 | ✅ | — | Core; CSV adds no dep; its blocker is satisfied | Zero `to_csv`/`to_parquet`/`export` in the package | Sidecar is now assembly, not authorship: `DICT_DF_SCHEMAS` + `get_provenance` both ship. Decide parquet-extra vs CSV-only |
| #483 | 2 · v1.4 | 🔄 | — | **Highest leverage here** — unblocks #498/#484/#40/#64 | Prior art inside pro (`_align_atom_values_to_target`, Biopython) | Core-or-pro is the blocking question: a core map may **not** import that aligner (pro + cross-class backend rules) |
| #484 | 2 · v1.4 | 🔄 | — | Fits but oversized and blocked (#22/#23/#40/#483) | `predict_samples(list_df_feat=)` + `ModelEvaluator` paired deltas give the machinery; tutorial 3e now teaches the fusion path | Re-scope to early fusion on paired deltas |
| #487 | 3 · v1.3 | 🔄 | — | Plain dict satisfies the no-schema-framework rule; `_data/` CONFIRM-FIRST | `_schemas.py` + `Overview.tsv` metadata ship | Smallest honest version: extend `Overview.tsv` + a `DICT_DATASET_CARDS` — no new public class. The work is curation, not code |
| #488 | 3 · v1.3 | ☑️ **45%** | — | Runner ours, ranking downstream | `AAPred.eval(baseline=, cv=)` + `ModelEvaluator.run` (same folds, per-fold `df_scores_`) + `predict_samples` | Rescope to: fold-level rows in the *returned* frame, runtime capture, a user-method hook. Land #25 first |
| #494 | 3 · v1.5 | 🔄 | — | CONFIRM-FIRST ×2 | **Mostly done** by `packaging.yml` + the matrix installs | Real gap is narrow: `[embed]` is installed by no workflow. Extras named in the issue (`[omics]`, GPU) **don't exist** — rescope to the 5 real ones |
| #490 · #273 | 3 · v1.4 | 🔄 | — | One omics feature split four ways; the readers are plain pandas so the `[omics]` gating is partly unjustified | Nothing; no `[omics]` extra exists | **One go/no-go on the whole track**, not four decisions |
| #489 · #491 | 3 · v1.4 | ⏸️ | — | #489 has no standalone deliverable; #491 is #273's own out-of-scope note promoted to an issue | Nothing | Park behind the track decision |
| #496 · #499 | 3 · v2.X | ⏸️ | — | v2 architecture; cross-cutting, not features | `_provenance.py` = the one-hop record; guardrails already binding rules | Keep #499 as the v2 anchor; **create no children until v2 planning opens**. Its `TaskSpec` child pre-empts #482's `task` field |
| #497 | 3 · v2.X | ❌→downstream | — | The border decision names **feedback data** as the downstream moat; nothing here produces or consumes an assay outcome | `get_provenance` covers the run-record half | Auditors split (❌ close vs ⏸️ park): a single-experiment record is a possible carve-out, but a public `aa.*` builder has zero in-package caller. **Maintainer call** |

---

## Reject / Defer appendix (with cited rule)

- **#497 ❌→downstream** — the border decision record lists *feedback data — which workflows
  actually solve users' problems* as the downstream moat, and its no-consumer rationale applies
  verbatim: don't create permanent semver surface with no in-package caller.
- **#492 ⏸️** — rejected-optimization list: *sparse one-hot encoding — a return-dtype/structure
  change, an API change, not a numerical-equivalence question; risky for a public encoder.*
  Compounded by there being no sparse input path at all.
- **#65 ⏸️** — scope test: *balloons the dependency surface* (three search tools + three aligners
  + a cloud API), far past the single-binary precedent set for the motif scanner.
- **#42 ⏸️** — scope test: *a research program rather than a package feature*, plus a
  CONFIRM-FIRST `_data/` rewrite of published scales.
- **#50 · #51 · #54 · #265 ⏸️** — dependency surface (imodels/alibi · captum + a DL runtime ·
  DoWhy/EconML/causal-learn/gCastle · gplearn/pysr). The border decision routes causal and neural
  attribution downstream; `torch` is deliberately isolated in `[embed]` to keep the DL stack out
  of `[pro]`.
- **#263 ⏸️** — no GPU runner exists in CI, so the issue's own KPI is unverifiable; output-affecting
  work additionally needs a declared tolerance tier and a frozen anchor.
- **#496 · #499 ⏸️** — v2 architecture; the typed-result child is gated by *don't introduce typed
  row records until the v2 migration*, and typed I/O contracts belong downstream.
- **#498 ⏸️** — hard-blocked by unbuilt #483; no rule conflict.
- **#489 · #491 ⏸️** — #491 fails the scope test twice (balloons the dep surface with
  anndata/scverse, and is not sequence-based protein prediction; pseudobulk is already
  Scanpy/decoupler's job). #489 has no standalone deliverable.
- **#64 🔄 (blocked)** — contradicts the standing decision to neither test nor integrate the
  orphaned conservation module; needs that decision explicitly superseded first.

---

## Housekeeping actions this refresh surfaced

1. **Milestone the 14 unmilestoned issues.** They are all new and all real work.
2. **Re-milestone what shipped early:** #480 (v1.3 → v1.2), #79 (v1.5 → v1.2), #93 (v1.3 → v1.2).
3. **Merge the four open PRs one at a time** — three of them edit `CHANGELOG.md` and
   `release_notes.rst` and will conflict as a batch.
4. **#482's `mcc` bullet is now stale** (shipped via #510) — delete it from the issue body.
5. **Kanban needs no action** — the gap that the last two refreshes reported is closed.
