# GitHub issues — implementation handoff

_Generated by the `github-issues` skill on **2026-07-14**. Regenerate:_
_`python3 .claude/skills/github-issues/scripts/fetch_issues.py`, then re-run the skill._

Audits every open issue against package scope (interpretable, CPP-centered, sequence-based
protein prediction; `pro` extra for heavy deps; semver-strict v1) and the standards in
`CLAUDE.md` + `.claude/rules/sharp-edges.md`. Verdicts: ✅ Ready · 🔄 Revisit
(decision/under-spec/oversized) · ⏸️ Defer-v2 · ❌ Reject (rule conflict) · ☑️ Done/Partial.

> **What changed since the 2026-07-10 refresh.**
> - **New work surfaced by the notebook-simplification / agent-readiness grill (2026-07-14):**
>   epic **#414** + children **#408** (`AAPred.score_to_group`), **#409** (capability-based
>   estimator validation), **#410** (bundled-dataset display metadata), **#411** (nested
>   `selection_scope` in `find_features`), **#412** (`ap.explain_features` enrichment),
>   **#413** (simplify the g-sec use case with shipped API). Plus the **AAPredPlot group-plot
>   cluster** #391/#392/#393.
> - **4 of the epic children are in flight right now** as open PRs: **#415→#408**, **#416→#409**,
>   **#417→#410**, **#418→#412** — and they collide (see the overlap cluster). **#389** modernizes
>   the g-sec Appendix-3 notebook (advances #109/#413/#414, no closing keyword).
> - **Closed since last pass:** **#340** (per-feature struct isolation + `encode()` router, PR
>   #406), **#390** (`group_cluster` split, #388), **#368** (CPP bootstrap, #371), **#219**
>   (`auto_font`, #378), **#397** (`eval(cv=)`, #400), **#398** (`predict_oof`, #399).
> - **Big non-issue program work merged:** the **v2 class-abbreviation rename** (`aap`→`ap`,
>   `AAlogo`→`AALogo`, …; PR #401, ADR-0057 — do not recommend the old abbreviations), settable
>   **`cell_size`** (#387), the **`sample_kws` bundle** + layout fixes (#404/#405), name-based
>   sample resolution (#407), and the `_kws` docs convention (#403).
> - **✅ Milestones: 0 unmilestoned** (the 10 new gaps — #391/#392/#393/#408–#414 — were assigned
>   2026-07-14; see the Milestones section).
>
> **Later on 2026-07-14 (v1.1 refresh).** The whole in-flight epic-#414 PR cluster merged
> (#415/#416/#417/#418), plus **#274/#421 packaging gates** (PR #420) and the **#345 param-coverage
> backlog** (PRs #422–#425 + #427, baseline deleted). All now **CLOSED**. **#336** epic's last child
> **#341** also closed → **#336 epic CLOSED (completed) 2026-07-14**. Epics **#106** and **#126** each
> now collapse to a single open child (#109, #131 respectively) — noted in a status comment on both;
> close each when its child lands. The **data-flow diagram lane closed** — #227 (infogram asset) and
> #226 (Usage Principles swap-in) were both already shipped/merged on master and verified complete.
> **#335** later closed (composition family shipped) and its ACC tail spun out to **#428**.
> **v1.1 open is now 8 issues** (#35, #75, #80, #106, #109, #126, #131, #428); **no open PRs**.
> Local occupied lane: the AAPredPlot prediction-plotting layer (`_aa_pred_plot.py` + `aa_pred_plot_*`
> backends + new `aa_pred_plot_legend.py`) + `aap_plot_*` notebooks + `use_case1` (#109 g-sec). The only remaining *code* gaps in v1.1 are
> **#335** (ACC featurizer) and **#131** (session plot style) + **#75** (logging); the rest are
> docs/epics. Working-tree note: `use_case1_gamma_secretase.ipynb`, `_aa_pred_plot.py`, and
> `cpp_plot_ranking.py` are **modified locally (uncommitted)** — the user is continuing g-sec/#109
> work in this checkout, so treat #109 and the prediction-plot files as an occupied lane.

## Snapshot
- **Open issues: 66** (was 57 on 2026-07-10; +10 new grill/group-plot issues, −1 closed #340). Verdict split:
  ✅ Ready **8** · 🔄 Revisit/partial **42** · ⏸️ Defer-v2 **1** · ❌ Reject **0** · ☑️ Done-or-Partial **5**
  (#26, #37, #59, #261, #336 shipped-but-open → close/re-scope; + **10** long-tail XAI issues parked
  at v2.X, counted under 🔄→ProtXplain).
- **In flight now (open PRs):** **#415** (#408 `score_to_group`), **#416** (#409 capability
  validation), **#417** (#410 dataset metadata), **#418** (#412 `explain_features` enrichment),
  **#389** (g-sec notebook modernization → advances #109/#413/#414). **These four epic-child PRs
  share files — merge them in a coordinated, rebased sequence (see overlap cluster).**
- **Recently resolved on master (verify before re-touching):** #340, #390, #368, #219, #397, #398.
- **Epic #336 essentially complete:** all 4 concrete children (#337/#338/#339/#340) are closed;
  only the **#335 ACC-featurizer tail** remains. Recommend closing #336 (or keep = #335 tracker).

## Issue ↔ PR activity (secondary)
> **Issues on the left** (the spine); their PR(s) on the right — **—** when an issue has no PR.
> `✅` = a PR closed the issue · `↗` = PR(s) advance it (no closing keyword / still open/in-flight).

| issue | state | PR(s) | what landed / in flight |
|---|---|---|---|
| #408 | 🔄 open | **#415 ↗** (open) | `AAPred.score_to_group` confidence-band classifier (static method) — in flight |
| #409 | 🔄 open | **#416 ↗** (open) | capability-based estimator validation (don't force `predict_proba`) — in flight |
| #410 | 🔄 open | **#417 ↗** (open) | `load_dataset(add_metadata=True)` gene/display_name/organism — in flight (CONFIRM-FIRST `_data/`) |
| #412 | 🔄 open | **#418 ↗** (open) | `ap.explain_features(add_sample_mean_dif=)` enrichment (pro) — in flight |
| #413 | 🔄 open | #389 ↗ | g-sec Appendix-3 rewrite to shipped API; blocked-by #408/#409/#410 landing |
| #414 | 🔄 epic | #415/#416/#417/#418/#389 ↗ | umbrella for the grill; 4 children in flight, #411/#413 pending |
| #109 | 🔄 open | #389 ↗ | g-sec use case = flagship Use Case; #389 modernizes it |
| #340 | ✅ CLOSED (2026-07-14) | #406 ✅ | per-feature failure isolation + `encode()` backend router |
| #390 | ✅ CLOSED | #388 ✅ | clustermap split into `AAPredPlot.group_cluster` |
| #368 | ✅ CLOSED | #371 ✅ | CPP bootstrap / stability annotation |
| #219 | ✅ CLOSED | #378 ✅ | `auto_font` consistency finish |
| #397 | ✅ CLOSED | #400 ✅ | `AAPred.eval(cv=)` pooled splitter |
| #398 | ✅ CLOSED | #399 ✅ | `AAPred.predict_oof` out-of-fold scores |
| #335 | 🔄 partial (open) | #360/#382 ✅ | featurizers + `eval(baseline=)` shipped; **only ACC remains** → kept as ACC tracker |
| #336 | 🔄 epic ~done (open) | #337/#338/#339/#340 ✅ | 4/4 concrete children closed; only #335 ACC tail left |
| #241 | 🔄 partial (open) | #244/#258/#266/#255/#375 ↗ | pipe facade shipped (evolved verbs); `SequenceFeatureTransformer` still unbuilt |
| #391 | 🔄 open | — | `kind="dendrogram"` (rect/circular) for `group_cluster` |
| #392 | 🔄 open | — | `AAPredPlot.group_map` 2D landscape (pro) |
| #393 | 🔄 open | — | `AAPredPlot.group_network` similarity graph (pro) |
| #411 | 🔄 open | — | nested `selection_scope="global"|"fold"` in `find_features` (needs ADR) |
| all other open issues | 🔄 open | — | no PR yet — see per-issue audit |

**Program / tooling PRs with no backing issue (recent):** #401 (v2 naming rename, ADR-0057),
#387 (`cell_size`), #404/#405 (furniture/layout + `sample_kws`), #407 (name-based sample resolve),
#403 (`_kws` docs), #402 (static-asset refresh), #394/#395/#396 (ranking/feature-map polish),
#383/#385/#386 (CPP seq-bar), #362/#367 (perf-gate/ADR-0054), #364/#372 (docstrings), #359 (`pad_parts`).

---

## ▶ What to work on next (recommended order)

**In-flight — land the epic-child PRs first, in a coordinated sequence (they share files):**
1. Merge **#415 (#408)** → rebase and merge **#416 (#409)** (both edit `prediction/_aa_pred.py`
   + `_constants.py`) → **#417 (#410)** (shares `_constants.py`/`release_notes.rst`; CONFIRM-FIRST
   `_data/`) → **#418 (#412)** (isolated except `release_notes.rst`). Do **not** merge them blindly
   in parallel — the second/third of each shared file will conflict.
2. **#389** g-sec notebook (shares `CONTEXT.md` with #415) — land after #415; it advances the
   **#413/#414** capstone and **#109** Use Case.

**Then — the grill tail that has no PR yet:**
3. **#413** — refactor g-sec Appendix 3 to the shipped API; blocked-by #408/#409/#410 landing
   (needs `score_to_group` + `predict_oof` + `feature_matrix(df_seq=)` all on master).
4. **#411** — nested `selection_scope` in `find_features`: **write the ADR first** (global vs
   fold selection; relates #276) — it changes a shipped, agent-facing "End" pipeline's reported
   numbers, so default must stay byte-identical (opt-in `selection_scope="fold"`).

**High-value, additive, builds on the shipped prediction layer:**
5. **#91 Model evaluation** (prio:1, v1.3) — repeated-CV + bootstrap CIs + paired ΔMCC; reuses
   `comp_bootstrap_ci` + `aaanalysis.metrics`; slots next to `AAPred.eval`. Decide the
   `ModelEvaluator(Tool)+Plot` vs. helpers shape first.
6. **#274 Packaging CI gates** (prio:1, v1.1) — wheel/sdist + install-from-wheel smoke + `__all__`
   import test **before the v1.1 release**. CONFIRM-FIRST (`.github/workflows/*`).

**Parallel doc lane (different files, safe alongside the above):**
7. **#345 param-coverage burn-down** — one PR per subpackage, drops rows from the baseline.
8. **#106 docs epic → #109 Use Cases / #227 infogram → #226** — doc-only pillar.

**Housekeeping — close/re-scope shipped-but-open issues:**
9. **#261 SeqOpt is shipped** (`SeqOpt`/`SeqOptPlot` public; ADR-0043/0045) — recommend **close it**
   or re-scope to NSGA-III/SPEA2 futures; its text still describes the superseded byte-exact
   framing. **#59** (multi-objective) substance is delivered by it. **#336** epic → close (4/4
   children done). These need a maintainer decision, not new code.

**Bigger, needs a design pass first (don't start cold):**
10. **#365 Structural CPP** (prio:2, v1.4) — extend CPP to 3D/conformational targets with
    DiscoTope/BepiPred benchmarks; large, shares the structure (pro) surface. Scope down first.

---

## Milestones — all assigned ✅ (0 gaps)

**Resolved 2026-07-14: the 10 previously-unmilestoned issues were assigned.** Every open issue now
carries a milestone (v1.1–v1.5 or v2.X). The new assignments:

| # | title (short) | milestone | why |
|---|---|---|---|
| 408 | `AAPred.score_to_group` | **v1.1** | additive AAPred quick win; PR #415 in flight |
| 409 | capability-based validation | **v1.1** | additive robustness; PR #416 in flight |
| 410 | dataset display metadata | **v1.1** | additive data onramp (feeds name-resolver); PR #417 in flight |
| 412 | `ap.explain_features` enrich | **v1.1** | additive pipe enrichment (pro); PR #418 in flight |
| 413 | simplify g-sec use case | **v1.1** | Use-Case/docs capstone; groups with #109 |
| 414 | g-sec + agent-readiness epic | **v1.1** | near-term API-trust umbrella (matches #336 at v1.1) |
| 411 | nested `selection_scope` | **v1.3** | evaluation-rigor feature; groups with #91/#276 |
| 391 | `group_cluster` dendrogram | **v1.4** | AAPredPlot static viz primitive |
| 392 | `group_map` 2D landscape (pro) | **v1.4** | static XAI viz (design frontier) |
| 393 | `group_network` graph (pro) | **v1.4** | static XAI viz (design frontier; new dep = CONFIRM-FIRST) |

Milestone health (open counts, post-assignment): **v1.1 ×19 · v1.2 ×5 · v1.3 ×16 · v1.4 ×10 ·
v1.5 ×5 · v2.X ×11** = 66.

---

## Overlap clusters — DO NOT develop in parallel
| Cluster | Issues / PRs | Primary | Why they collide |
|---|---|---|---|
| **g-sec epic in-flight** | **PR #415/#416** (`_aa_pred.py`), **#415/#416/#417** (`_constants.py`, `release_notes.rst`), **#415/#389** (`CONTEXT.md`) | #415 | 4 open PRs share files → merge in a rebased sequence, not blind-parallel |
| **AAPredPlot group plots** | #391, #392, #393 | #391 | all add `AAPredPlot.group_*` on the `group_cluster` (#388) base — one owner |
| **Feature-selection rigor** | #411, #276, #91 | #91 | `find_features` CV + nested selection + model-eval all touch the evaluation surface |
| **Prediction / baseline** | #335, #91, #276 | #335 | all edit `prediction/_aa_pred.py` (`eval`) + `SequenceFeature` featurizers |
| **`aaanalysis.pipe` facade** | #241, #126 | #241 | evolved verbs in `aaanalysis/pipe/*`; transformer + naming reconciliation = one owner |
| **Protein design / opt** | #37, #261✅, #59✅, #60 | #60 | shared `protein_engineering/`; #261's `SeqOpt` engine (subsuming #59) is **already shipped** — only #60 active-learning remains open work here |
| **Structure (pro)** | #40, #65, #42, #64 | #40 | share `data_handling_pro` / `feature_engineering_pro` + the pending pro move |
| **Docs infogram** | #226, #227 | #227 | #226 page-swap blocked-by #227 artwork |
| **Docs epic** | #106, #109, #35, #80 | #106 | one doc owner; nav + pillars |
| **Embedding / XAI** | #22, #23, #47 | #22 | shared embedding surface |

---

## Per-issue audit

### g-sec agent-readiness grill (#414 epic) — the active frontier
| # | prio | ms | verdict | note (complements the issue) |
|---|---|---|---|---|
| 414 | 2 | v1.1 | 🔄 epic | Umbrella; 4 children in flight (#408/#409/#410/#412), #411/#413 pending. Keep as tracker. |
| 408 | 2 | v1.1 | ✅ Ready (PR #415) | `AAPred.score_to_group` = **static method** (threshold→ordered confidence-band); edits `_aa_pred.py`+`_aa_pred_plot.py`+backend. Collides with #416. |
| 409 | 2 | v1.1 | ✅ Ready (PR #416) | Capability-based validation: allow estimators without `predict_proba` for hard-label metrics. Edits `_aa_pred.py` — collides with #415. |
| 410 | 2 | v1.1 | ✅ Ready (PR #417) | `load_dataset(add_metadata=True)` gene/display_name/organism. **CONFIRM-FIRST `_data/`** (adds `DOM_GSEC.tsv` metadata). Note: organism deferred (no offline species source per prior grill). |
| 411 | 2 | v1.3 | 🔄 Revisit | Honest nested-CV `selection_scope="global"\|"fold"` in `find_features` (`_find_features.py:527`/`:182`). **Default byte-identical** (opt-in fold). **Write the ADR first** (relates #276). No PR yet. |
| 412 | 3 | v1.1 | ✅ Ready (PR #418) | `ap.explain_features(add_sample_mean_dif=, label_ref=)` returns enriched `df_feat` (pro). Isolated PR (`pipe/_explain_features.py`). |
| 413 | 2 | v1.1 | 🔄 docs (blocked) | Refactor g-sec App3 (~255→~200 lines) to `predict_oof`/`feature_matrix(df_seq=)`/dPULearn split-input; **blocked-by #408/#409/#410 landing**. #389 is the in-flight notebook PR. |

### AAPredPlot group-plot cluster (static viz)
| # | prio | ms | verdict | note |
|---|---|---|---|---|
| 391 | 3 | v1.4 | 🔄 | `kind="dendrogram"`, `layout="rectangular"\|"circular"` on `group_cluster`; linkage already computed for the clustermap (#388) — low cost. |
| 392 | 3 | v1.4 | 🔄 (pro) | `group_map` 2D protein landscape (embedding + volcano) with subset highlighting. New pro viz surface. |
| 393 | 3 | v1.4 | 🔄 (pro) | `group_network` sample-similarity graph. Likely a **new graph dep** (networkx) → CONFIRM-FIRST `pyproject.toml`; keep opt-in `pro`. |

### Prediction / evaluation / design
| # | prio | ms | verdict | note |
|---|---|---|---|---|
| 335 | 2 | v1.1 | ☑️ **CLOSED** | Composition family shipped (scale/AAC/DPC/kmer + `CPP.run_composit` + `eval(baseline=)`, #360/#382). ACC tail carved out to **#428**. |
| 428 | 3 | v1.1 | 🟢 **IMPLEMENTED (local `feat/428-acc` @ a3aa5ce5)** | `SequenceFeature.acc` = auto-covariance only over full default scales (`n_scales·n_lag`); `STR_BASELINE_ACC` + `AAPred.eval` wired; 36 tests + notebook + release note. Verified green. No CONFIRM-FIRST surface. **Independent lane** (no config.py/plotting.py). Awaiting review/push. |
| 336 | 2 | v1.1 | ☑️ **CLOSED (completed)** | **All 5 concrete children closed** (#337–#341). Epic closed 2026-07-14; ACC tail tracked on #335, minor items fold into #106. |
| 91 | 1 | v1.3 | 🔄 Ready-ish | Decide `ModelEvaluator(Tool)`+Plot vs. helpers. Reuse `comp_bootstrap_ci`+`metrics`; sits beside `AAPred.eval`. Groups with #411/#276. |
| 276 | 3 | v1.3 | 🔄 Revisit | Paper-fidelity nested-CV Monte-Carlo + ensembles behind `predict_samples`. Oversized — scope down; overlaps #411's nested-CV. |
| 37 | 1 | v1.2 | ☑️ merged, kept open | AAMut/SeqMut merged; the design gate for #57/#59/#60/#261. Decide "done" vs. remaining design verbs. |
| 261 | 1 | v1.2 | ☑️ **SHIPPED, kept open** | **`SeqOpt`/`SeqOptPlot` are shipped and public** (`protein_engineering/_seqopt.py`, in `__all__`; modes `impact`/`importance`, ADR-0043/0045). The issue text still describes the **superseded** byte-exact `mode="exact"/"fast"` NSGA-II parity framing (ADR-0045 dropped it for "equivalence, single vectorized kernel, ship-ours"). **Recommend: close #261 or re-scope to the NSGA-III/SPEA2 futures only.** |
| 59 | 1 | v1.2 | ☑️ → #261 shipped | Multi-objective substance delivered by the shipped `SeqOpt`. Don't auto-close (maintainer decision) — but the engine exists. |
| 60 | 2 | v1.4 | 🔄 | Active learning; after #261. |
| 16 | 1 | v1.2 | 🔄 | Uncertainty over CPP features; coordinate with #53 + shipped `ReliabilityModel` (check overlap first). |
| 93 | 3 | v1.3 | 🔄 | Learning-curve "sampling-limited?"; builds on #91. |

### Structure (pro) + remaining #336 tail
| # | prio | ms | verdict | note |
|---|---|---|---|---|
| 40 | 2 | v1.3 | 🔄 pro | DSSP→scales; coordinate the pending `data_handling_pro` move. |
| 65 | 3 | v1.5 | 🔄 pro | `get_msa` misnamed; real homolog+align backends. |
| 42 | 3 | v1.5 | 🔄 | PTM/structure scales touch `_data/` (CONFIRM-FIRST). |
| 64 | 3 | v1.4 | ⏸️ | Conservation feature deferred (ADR-0012); blocked on #65 + pro move. |
| 365 | 2 | v1.4 | 🔄 Revisit (big) | Structural CPP for 3D/conformational targets + DiscoTope/BepiPred benchmarks. Large. |

### Pipe / ergonomics / API
| # | prio | ms | verdict | note |
|---|---|---|---|---|
| 241 | 2 | v1.2 | 🔄 partial | Facade shipped (`find_features`/`predict_samples`/`explain_features`/`plot_eval`/`obtain_samples`). **Remaining: `SequenceFeatureTransformer`** (sklearn, leak-free) + reconcile issue text with shipped verbs. |
| 126 | 2 | v1.1 | 🔄 epic | Drive children; the pipe API is its realization. |
| 131 | 2 | v1.1 | 🟢 **IMPLEMENTED (local `feat/131-plot-settings` @ 884b4e27)** | `options['plot_settings']` applied lazily via `plot_gco`; unset → byte-identical (39 visual-regression tests unchanged). **CONFIRM-FIRST `config.py`** edited. **Merge-couples with #75** (both edit `_utils/plotting.py` `plot_gco` + adjacent `config.py` imports → serialize). Awaiting review/push. |
| 87 | 3 | v1.3 | 🔄 | Named CPP strategy preset; doc-first. |
| 75 | 3 | v1.1 | 🟢 **IMPLEMENTED (local `feat/75-logging` @ 041d454e)** | `print_out`→`logging.getLogger("aaanalysis")`; `set_logger_verbosity` mapped from `check_verbose`; 12 caplog tests. **CONFIRM-FIRST `config.py`** edited. `display_df` shape-print left un-routed on purpose (grep=1 not 0). **Merge-couples with #131** (`plot_gco` + `config.py`). Awaiting review/push. |

### CI / packaging / docs
| # | prio | ms | verdict | note |
|---|---|---|---|---|
| 274 | 1 | v1.1 | ☑️ **CLOSED** | Packaging gates shipped (PR #420, bc14cc12): wheel+sdist build, install-from-wheel/sdist smoke, `__all__` import test, `MANIFEST.in` ships `*.pyx`. #421 folded in. |
| 345 | ? | v1.1 | ☑️ **CLOSED** | Param-coverage backlog burned to 0 across 5 subpackage PRs (#422/#423/#424/#425/#427); baseline file deleted → gate is now a permanent zero-gap check. |
| 106 | 1 | v1.1 | 🔄 epic | Docs-architecture parent; one owner. |
| 109 | 2 | v1.1 | 🔄 | Examples › Use Cases (g-sec shipped; #389/#413 modernize it). Confirm boundary vs protocols. |
| 227 | 3 | v1.1 | ☑️ **CLOSED** | Infogram shipped: `_artwork/diagrams/dataflow_map.png` + editable `_static/dataflow_map.html` + `.pdf` vector; all labels verified against public API. (Vector is PDF not `.svg` — optional parity follow-up.) |
| 226 | 3 | v1.1 | ☑️ **CLOSED** | Usage Principles consumes the infogram; `components.png`/`connections.png` retired to `legacy/`, prose links the ecosystem map; one data-flow figure remains. |
| 80 | 3 | v1.1 | ✅ | seaborn-style tutorial gallery; new docs dep = CONFIRM-FIRST. |
| 35 | 2 | v1.1 | ☑️ ~done | Protocols pillar shipped (`protocols.rst` grid + `protocol1_cpp_signature`…`protocol10_validation`, core 4 present). Tail = P0–P12 renumber + a couple pipeline protocols (scale-selection). |

### Data / schema / ecosystem
| # | prio | ms | verdict | note |
|---|---|---|---|---|
| 29 | 2 | v1.3 | ✅ | Normalization appended via `sort_cols_feat`; serialize ahead of #33/#26. |
| 33 | 2 | v1.3 | ✅ | CSV/parquet export of `df_feat`; canonical `LIST_COLS_FEAT`. |
| 26 | 3 | v1.3 | ☑️ partial | df_feat + family contracts documented (#234/#238); remaining = per-residue-score shape + stable ProtXplain anchor. |
| 28 | 2 | v1.3 | 🔄 | Sliding-window CPP on `AAWindowSampler`; overlaps #27. |
| 27 | 3 | v1.3 | 🔄 | Custom-region abstraction; coordinate output schema. |
| 25 | 3 | v1.3 | 🔄 | Curate benchmark dataset (`_data/` CONFIRM-FIRST). |
| 22 | 2 | v1.3 | 🔄 partial | Embedding input landed (`[embed]`); gap = pooled-vector fusion with CPP. |
| 23 | 3 | v1.4 | 🔄 | CPP↔embedding correlation; pairs with #22. |
| 210 | 3 | v1.4 | 🔄 epic | Ecosystem integration; drive children; no heavy core deps. |
| 273 | 3 | v1.4 | 🔄 | scverse/AnnData adapters via `[omics]` extra (CONFIRM-FIRST pyproject). |
| 263 | 3 | v1.5 | 🔄 | GPU CPP backend; opt-in, CPU default. |

### CPP core / features (non-XAI)
| # | prio | ms | verdict | note |
|---|---|---|---|---|
| 45 | 3 | v1.3 | 🔄 | Motif integration into CPP; FIMO p/q overlay → `df_feat`. |
| 46 | 3 | v1.5 | 🔄 | Functional alignment; CPP-distance in-scope, rest → ProtXplain. |
| 36 | 3 | v1.3 | 🔄 | New scale-selection method; needs a concrete spec. |
| 89 | 3 | v1.3 | 🔄 | Between-residues/bond-centered features; on AAWindowSampler. |
| 79 | 3 | v1.5 | 🔄 | PSSM = `(L,20)`→`dict_num` glue into `run_num`. |
| 44 | 3 | v2.X | 🔄 | Temporal CPP (ΔCPP/Δt); needs a time-point data contract. |

### XAI long-tail (mostly ProtXplain-scoped, v2.X)
| # | prio | ms | verdict | note |
|---|---|---|---|---|
| 56 | 2 | v1.4 | 🔄 | Viz layer: static parts in-core; interactive → ProtXplain. |
| 55 | 3 | v2.X | 🔄 | Fidelity/stability **metrics slice → `aaanalysis/metrics/`** (in-core); DL-XAI bench → ProtXplain. |
| 47 | 3 | v2.X | 🔄 | Feature attribution → CPP scale space. |
| 48 | 3 | v2.X | 🔄 | Concept-based: cluster CPP scales (AAclust). |
| 265 | 3 | v2.X | 🔄 | Symbolic-regression surrogate for fitness rules; heavy-dep, research. |
| 49–54 | 3 | v2.X | 🔄 → ProtXplain | example-based / rule / neural / surrogate / uncertainty-aware / causal XAI — heavy deps; keep out of core (`pro-core-boundary.md`). #53 merges intent with #16. |

## ADR health (2026-07-14 audit + naming fixes applied)

All 58 ADRs (0034 is an intentional gap) audited for staleness vs shipped code. **53 were current;
5 had drifted names** (decisions still held — **no decision was silently reversed**). Root cause: the
`protein_design` → `protein_engineering` subpackage rename and the v2 abbreviation rename (ADR-0057).
**All 5 have now been made consistent with the current naming** (hygiene check: 0 defects; INDEX
regenerated):

| ADR | was | fix applied |
|---|---|---|
| **0022** | Taxonomy forbids "sequence level" (also `CONTEXT.md:68`), but shipped `AAPred.predict` uses `level='sequence'/'domain'/'window'`. | Added a dated **amendment** mapping `sequence`↔protein, `window`↔residue (kept the immutable decision; the glossary term stays "protein level"). **Open call for the maintainer:** amend-only (done) vs. realign the API param names (breaking). |
| **0040** | Named the pipe Ends `predict_labels` / `sample_windows`. | Renamed inline to shipped `predict_samples` / `obtain_samples` + dated amendment note; `design_mutations`/`evaluate_models` left flagged as planned. |
| **0043** | `SeqOpt` in `protein_design_pro/` → core `protein_design/`. | Amendment + consequences now say core `protein_engineering/`; `DEDICATED_OWNERS` owner corrected. (Original D1's reversed `*_pro` proposal kept as the immutable historical record.) |
| **0027, 0042** | "`protein_design` stays in core". | → `protein_engineering`. |
| **0033** | `AAlogo` + a `protein_design` path (also `AAlogoPlot` in 0006/0016/0039). | → `AALogo` / `AALogoPlot` / `protein_engineering`. |

Also noted (not an ADR issue, left untouched): two orphaned empty legacy dirs `aaanalysis/protein_design/`
and `aaanalysis/protein_design_pro/` with no `__init__.py` — a code-hygiene cleanup that **needs explicit
deletion permission** per hard rule §0.

## Defer / Reject appendix (with cited rule)
- **#64 ⏸️** — deferred by **ADR-0012**; blocked on #65 + the pro move.
- **v2.X XAI suite (#47–#55) / #265 / #54 / #51** — not rule-violations, but balloon the
  dependency surface (captum/DoWhy/PyMC/alibi/imodels) or belong to **ProtXplain**
  (`pro-core-boundary.md`). Keep the boundary; #55's *metrics* slice stays in-core.
- **#273 / #263 / #261-`deap` / #393-graph-dep** — new-dependency asks → `[omics]`/`[gpu]`/`[dev]`/
  `pro` extras, CONFIRM-FIRST `pyproject.toml`; never a required core dep.
- No issue currently triggers a hard-rule **Reject** (no `AAanalysisError`/SECURITY.md/ruff/mypy asks).
