# GitHub issues — implementation handoff

_Generated by the `github-issues` skill on **2026-07-10**. Regenerate:_
_`python3 .claude/skills/github-issues/scripts/fetch_issues.py`, then re-run the skill._

Audits every open issue against package scope (interpretable, CPP-centered, sequence-based
protein prediction; `pro` extra for heavy deps; semver-strict v1) and the standards in
`CLAUDE.md` + `.claude/rules/sharp-edges.md`. Verdicts: ✅ Ready · 🔄 Revisit
(decision/under-spec/oversized) · ⏸️ Defer-v2 · ❌ Reject (rule conflict) · ☑️ Done/Partial.

> **What changed since the 2026-06-24 refresh.** The entire **prediction layer landed**:
> `AAPred`/`AAPredPlot` (#344), `ReliabilityModel` (#358), `dPULearn.project` (#357/#352 ✅),
> CPP `bootstrap` stability (#371/#368 ✅), CPP `redundancy="exact"` (#351) + the three #343
> defects (#351/#353/#356), `auto_font` consistency (#378/#219 ✅), composition featurizers
> (#360), and the `aap` pipe verbs (`find_features`/`predict_samples`/`explain_features`/
> `plot_eval`/`obtain_samples`). Old "in progress" spine items (#132/#133/#134/#241-partial)
> are superseded below.
>
> **Since 2026-07-09:** `AAPred.eval(baseline=)` shipped (**#382**, part of #335 — ACC tail still
> open); `aa.plot_rank` **removed**, folded into `AAPredPlot.predict_group(kind="rank_scatter")`
> (**#384**, breaking, ADR-0056; do not recommend `aa.plot_rank`); g-sec docs (#381) + CPP
> sequence-bar polish (#383). **1 open PR:** #385 (CPP seq-bar bottom margin, another session).

## Snapshot
- **Open issues: 57** (was 59; **#342 and #288 closed 2026-07-09** — verified already-resolved,
  see below). Verdict split (this audit): 🔄 partial-advanced **4** (#345/#335/#336/#241) · the
  rest 🔄 Revisit / ✅ Ready / ⏸️ Defer-v2 (see per-issue table).
- **Closed this pass (verified complete):**
  - **#342 ✅ CLOSED** — batch shipped in PR #346 (+14 regression tests) and #350; the three
    plotting items were **permanently rejected in ADR-0049** (profile y-padding, feature_map
    `show_only_max`, `weight_bold` spine widths — each with an inline note). Nothing remained.
  - **#288 ✅ CLOSED** — Stages A–D + `output=` selection shipped via #285/#291/#294/#296
    (`map_structure`/`plot_combined`/`interactive`/`explore`). Functionally complete.
- **Shipped this pass (issue stays open):** **#335** — `AAPred.eval(baseline=)` merged (**#382**,
  `ce2ebdb7`); only the **ACC** featurizer remains, so #335 is **kept open as the ACC tracker**.
- **In flight now (open PRs):** **#385** — CPP sequence-bar bottom margin (another session; no
  backing open issue).
- **✅ Milestones: 0 unmilestoned** — the 14 gaps flagged last pass were all assigned 2026-07-09
  (v1.1 ×5, v1.2 ×2, v1.3 ×1, v1.4 ×2, v1.5 ×1, v2.X ×1; #342/#288 closed instead).

## Issue ↔ PR activity (secondary)
> **Issues on the left** (the spine); their PR(s) on the right — **—** when an issue has no PR.
> `✅` = a PR closed the issue · `↗` = PR(s) advance it (no closing keyword / still partial).

| issue | state | PR(s) | what landed / remaining |
|---|---|---|---|
| #342 | ✅ CLOSED (2026-07-09) | #346, #350 ✅ | correctness batch shipped; 3 plot items rejected via ADR-0049 |
| #288 | ✅ CLOSED (2026-07-09) | #285, #291–#296 ✅ | linked structure explorer complete (Stages A–D + output=) |
| #345 | 🔄 partial (open) | #363 ↗ | param-coverage backlog **257 → 95** rows; 95 remain (mostly pro notebooks) |
| #335 | 🔄 partial (open) | #360/#319 ↗, **#382 ✅ merged** | featurizers (scale/AAC/DPC/kmer) shipped; **`AAPred.eval(baseline=)` now merged (#382)** with a conditional `features` col + hue-aware bars. **Only the ACC featurizer remains** (deferred; `n_scales²` needs a curated small scale-set) → issue kept open as the ACC tracker |
| #336 | 🔄 epic partial (open) | #347/#348/#349 ✅ (#337/#338/#339) | 3 of 5 children merged; **#340/#341/#335 remain** |
| #241 | 🔄 partial (open) | #244/#258/#266/#255/#375 ↗ | pipe facade shipped under **evolved verb names**; **`SequenceFeatureTransformer` still unbuilt** |
| #340 | 🔄 partial (open) | — | `on_failure` per-*row* policy exists; **per-*feature* isolation + unified `encode(features=)` router not done** |
| all other open issues | 🔄 open | — | no PR yet — see per-issue audit |

**Program PRs with no backing issue (recent):** #359 (`pad_parts`), #362/#367 (perf-gate/ADR),
#364/#372 (docstrings), #366 (`level='sequence'` rename), #373/#370/#381 (use-case/g-sec notebooks),
#374/#376/#377/#379/#380/#383/#385 (CPP plot polish + bootstrap refactor + seq-bar),
**#384** (`aa.plot_rank` removed → `AAPredPlot.predict_group(kind="rank_scatter")`, ADR-0056).

---

## ▶ What to work on next (recommended order)

**Housekeeping — all done 2026-07-09/10 (context):**
1. ✅ **#342 and #288 closed** (verified already-resolved; #342 shipped/rejected via ADR-0049).
2. ✅ **All 14 unmilestoned issues assigned** (0 gaps now).
3. ✅ **#335 `AAPred.eval(baseline=)` shipped** (#382) — the ACC featurizer is the only remaining
   #335 item (deferred; issue kept open). If picked up: ACC needs a **curated small default
   scale-set** first (avoids the `n_scales²` blow-up), then wire `AAPred.eval(baseline="acc")`.

**Then — high-value, additive, builds on the just-shipped prediction layer:**
4. **#91 Model evaluation** (prio:1, v1.3) — repeated-CV + bootstrap CIs + paired ΔMCC. Reuses
   `comp_bootstrap_ci` + `aaanalysis.metrics`; slots directly next to the new `AAPred.eval`.
   Decide option 2 (a `ModelEvaluator` Tool+Plot) vs. helpers first.
5. **#274 Packaging CI gates** (prio:2, v1.1) — wheel/sdist build + install-from-wheel smoke +
   `__all__` import test **before the v1.1 release**. CONFIRM-FIRST (`.github/workflows/*`).

**Parallel lane — docs/mechanical (different files, safe alongside the above):**
6. **#345 param-coverage burn-down** — one PR per subpackage, drops rows from the baseline.
   Pure notebook/docs work; the ideal parallel-session lane.
7. **#106 docs epic → #109 Use Cases / #227 infogram → #226** — doc-only pillar.

**Bigger, needs a design pass first (don't start cold):**
8. **#261 SeqOpt** (prio:1) blocked-by **#37/#259** (model-aware `SeqMut`) — largest new
   feature; parity-first NSGA-II. Settle #37 (finish AAMut/SeqMut, currently merged-but-open)
   and the model-aware `SeqMut` gate before opening this. #59/#60 fold into this lane.

---

## Milestones — all assigned ✅ (0 gaps)

**Resolved 2026-07-09: every open issue now carries a milestone** (the 14 previously-unmilestoned
issues were assigned; #342/#288 closed instead). The assignments applied:

| # | title (short) | milestone | why |
|---|---|---|---|
| 335 | baseline featurizers | **v1.1** | additive to shipped AAPred; small |
| 336 | epitope usability epic | **v1.1** | umbrella for 335/340/341; near-term |
| 340 | StructurePreprocessor isolation | **v1.4** | pro/structure lane |
| 345 | param-coverage burn-down | **v1.1** | docs debt gating the v1.1 quality bar |
| 226 | swap data-flow infogram | **v1.1** | docs onramp (blocked-by #227) |
| 227 | data-flow infogram artwork | **v1.1** | blocks #226 |
| 241 | `aaanalysis.pipe` (aap) | **v1.2** | facade mostly shipped; transformer tail |
| 261 | SeqOpt optimizer | **v1.2** | groups with #37/#59/#60 design lane |
| 263 | GPU CPP backend | **v1.5** | opt-in perf, long-horizon |
| 265 | symbolic-regression surrogate | **v2.X** | research/XAI, heavy-dep |
| 276 | nested-CV MC engine | **v1.3** | rigor layer atop predict_samples |
| 365 | structural CPP (3D targets) | **v1.4** | large; DiscoTope/BepiPred bench |

Everything else is milestoned (v1.1 ×8, v1.2 ×3, v1.3 ×14, v1.4 ×6, v1.5 ×4, v2.X ×10).

---

## Overlap clusters — DO NOT develop in parallel
| Cluster | Issues | Primary | Why they collide |
|---|---|---|---|
| **Prediction / baseline** | #335, #91, #276 | #335 | all edit `prediction/_aa_pred.py` (`eval`) + `SequenceFeature` featurizers |
| **`aaanalysis.pipe` facade** | #241, #126 | #241 | evolved verbs live in `aaanalysis/pipe/*`; the transformer + naming reconciliation is one owner |
| **Protein design / opt** | #37, #261, #59, #60 | #37 | shared `protein_engineering/` mutation API; #261 is the multi-objective engine that subsumes #59 |
| **Structure (pro)** | #288, #340, #40, #65, #42, #64 | #340 | share `data_handling_pro` / `feature_engineering_pro` + the pending pro move |
| **CPP plot surface** | #342-plot items, any new `_cpp_plot.py` work | — | `_backend/cpp/cpp_plot_*.py` — serialize |
| **Docs infogram** | #226, #227 | #227 | #226 page-swap blocked-by #227 artwork |
| **Docs epic** | #106, #107, #108✅, #109, #35, #80 | #106 | one doc owner; nav + pillars |
| **Embedding / XAI** | #22, #23, #47 | #22 | shared embedding surface |

---

## Per-issue audit

### Prediction / evaluation / design (the active frontier)
| # | prio | ms | verdict | note (complements the issue) |
|---|---|---|---|---|
| 335 | 2 | v1.1 | 🔄 partial | scale/AAC(`aa_composition`)/DPC(`dipeptide_composition`)/kmer **all shipped** (#360/#319). **`AAPred.eval(df_seq=, baseline=True\|kind\|list)` SHIPPED (#382, `ce2ebdb7`)** — builds the baseline `X` internally, appends rows tagged by a conditional `features` col (byte-identical when unset; `AAPredPlot.eval` bars hue by it). **Only ACC(`n_lag`) remains** — deferred (curated small scale-set needed); issue kept open as the ACC tracker. |
| 91 | 1 | v1.3 | 🔄 Ready-ish | Decide **option 2** (`ModelEvaluator(Tool)`+Plot) vs. helpers. Reuse `comp_bootstrap_ci`+`metrics`; sits beside new `AAPred.eval`. |
| 276 | 3 | — | 🔄 Revisit | Paper-fidelity nested-CV Monte-Carlo + ensembles behind `predict_samples`. AAPred has ensemble-averaging but **no nested-CV MC**. Oversized — scope down first. |
| 37 | 1 | v1.2 | ☑️ merged, kept open | AAMut/SeqMut merged; kept open as the design gate for #57/#59/#60/#261. Decide "done" vs. remaining design verbs. |
| 261 | 1 | — | 🔄 Revisit (big) | SeqOpt NSGA-II parity-first. Blocked-by #37/#259. `deap` → `[dev]` only (CONFIRM-FIRST pyproject); new `SeqOpt`/`SeqOptPlot` (CONFIRM-FIRST `__all__`). |
| 59 | 1 | v1.2 | 🔄 → #261 | Multi-objective ranking — substance folded into #261; don't build twice. Don't auto-close (maintainer decision). |
| 60 | 2 | v1.4 | 🔄 | Active learning; after #261. |
| 16 | 1 | v1.2 | 🔄 | Uncertainty over CPP features; coordinate with #53 + `ReliabilityModel` (already shipped — check overlap first). |
| 93 | 3 | v1.3 | 🔄 | Learning-curve "sampling-limited?"; builds on #91. |

### Usability epic #336 + structure (pro)
| # | prio | ms | verdict | note |
|---|---|---|---|---|
| 336 | 2 | — | 🔄 epic partial | 3/5 children merged (#337/#338/#339). Remaining: **#340, #341, #335**. Keep as tracker. |
| 340 | 2 | — | 🔄 partial | `on_failure` per-row exists; **add per-*feature* (column) NaN isolation + one `encode(features=[...])` router** over dssp/pdb/pae + a "msms not found → depth unavailable" warning. |
| 288 | 1 | — | ☑️ shipped | `map_structure`/`plot_combined`/`interactive`/`explore` all present → close. |
| 40 | 2 | v1.3 | 🔄 pro | DSSP→scales; coordinate pending `data_handling_pro` move. |
| 65 | 3 | v1.5 | 🔄 pro | `get_msa` misnamed; real homolog+align backends. |
| 42 | 3 | v1.5 | 🔄 | PTM/structure scales touch `_data/` (CONFIRM-FIRST). |
| 64 | 3 | v1.4 | ⏸️ | Conservation feature deferred (ADR-0012); blocked on #65 + pro move. |
| 365 | 2 | — | 🔄 Revisit (big) | Structural CPP for 3D/conformational targets + DiscoTope/BepiPred benchmarks. Large; likely v2.X. |

### Pipe / ergonomics / API
| # | prio | ms | verdict | note |
|---|---|---|---|---|
| 241 | 2 | — | 🔄 partial | Facade shipped as `find_features`/`predict_samples`/`explain_features`/`plot_eval`/`obtain_samples` (verbs diverged from the issue's `cpp_feature_map`/`predict`/`explain`). **Remaining: `SequenceFeatureTransformer` (sklearn, leak-free)** + reconcile the issue text with shipped names. |
| 126 | 2 | v1.1 | 🔄 epic | Drive children; the pipe API is its realization. |
| 131 | 2 | v1.1 | 🔄 | Session-persistent plot style (CONFIRM-FIRST `config.py`); unset → byte-identical. |
| 87 | 3 | v1.3 | 🔄 | Named CPP strategy preset; doc-first. |
| 75 | 3 | v1.1 | 🔄 | Route output through logging, keep `print_out` shim. |

### CI / packaging / correctness
| # | prio | ms | verdict | note |
|---|---|---|---|---|
| 342 | 2 | — | ☑️ near-done | PR #346 landed the batch. Verify `_seq_preproc` min_window_size + accept the intentional `cpp_plot_profile` no-op, then **close**. |
| 274 | 2 | v1.1 | ✅ Ready | Wheel/sdist + install-from-wheel + `__all__` import gate before v1.1. CONFIRM-FIRST workflows. |
| 345 | ? | — | 🔄 partial | Baseline 257 → **95** (PR #363). Burn down per-subpackage; docs-only; ideal parallel lane. |

### Docs (Lane E, doc-only)
| # | prio | ms | verdict | note |
|---|---|---|---|---|
| 106 | 1 | v1.1 | 🔄 epic | Docs-architecture parent; one owner. |
| 109 | 2 | v1.1 | 🔄 | Examples › Use Cases (first case study shipped as gamma-secretase; confirm boundary vs protocols). |
| 227 | 3 | — | 🔄 | Data-flow infogram artwork; blocks #226. |
| 226 | 3 | — | 🔄 | Swap infogram into `usage_principles.rst`; blocked-by #227. |
| 80 | 3 | v1.1 | ✅ | seaborn-style tutorial gallery; new docs dep = CONFIRM-FIRST. |
| 35 | 2 | v1.1 | ✅ | Protocols epic (mostly shipped); tail = scale-selection protocol. |

### Data / schema / ecosystem
| # | prio | ms | verdict | note |
|---|---|---|---|---|
| 29 | 2 | v1.3 | ✅ | Normalization appended via `sort_cols_feat`; serialize ahead of #33/#26. |
| 33 | 2 | v1.3 | ✅ | CSV/parquet export of `df_feat`; canonical `LIST_COLS_FEAT`. |
| 26 | 3 | v1.3 | ☑️ partial | df_feat + family contracts documented (#234/#238); remaining = per-residue-score shape + stable ProtXplain anchor. |
| 28 | 2 | v1.3 | 🔄 | Sliding-window CPP on `AAWindowSampler`; overlaps #27. |
| 27 | 3 | v1.3 | 🔄 | Custom-region abstraction; coordinate output schema. |
| 25 | 3 | v1.3 | 🔄 | Curate benchmark dataset (`_data/` CONFIRM-FIRST). |
| 22 | 2 | v1.3 | 🔄 partial | Embedding input landed (`[embed]`); gap = pooled-vector fusion with CPP. |
| 23 | 4 | v1.4 | 🔄 | CPP↔embedding correlation; pairs with #22. |
| 210 | 3 | v1.4 | 🔄 epic | Ecosystem integration; drive children; no heavy core deps. |
| 273 | 3 | v1.4 | 🔄 | scverse/AnnData adapters via `[omics]` extra (CONFIRM-FIRST pyproject). |
| 263 | 3 | — | 🔄 | GPU CPP backend; opt-in, CPU default. |

### CPP core / features (non-XAI)
| # | prio | ms | verdict | note |
|---|---|---|---|---|
| 45 | 3 | v1.3 | 🔄 | Motif integration into CPP; FIMO p/q overlay → `df_feat`. |
| 46 | 3 | v1.5 | 🔄 | Functional alignment; CPP-distance in-scope, rest → ProtXplain. |
| 36 | 3 | v1.3 | 🔄 | New scale-selection method; needs a concrete spec. |
| 89 | 3 | v1.3 | 🔄 | Between-residues/bond-centered features; on AAWindowSampler. |
| 79 | 3 | v1.5 | 🔄 | PSSM = `(L,20)`→`dict_num` glue into `run_num`. |
| 44 | 3 | v2.X | 🔄 | Temporal CPP (ΔCPP/Δt); needs a time-point data contract. |
| 42/64 | — | — | see structure block above |

### XAI long-tail (mostly ProtXplain-scoped)
| # | prio | ms | verdict | note |
|---|---|---|---|---|
| 56 | 2 | v1.4 | 🔄 | Viz layer: static parts in-core; interactive → ProtXplain. |
| 55 | 3 | v2.X | 🔄 | Fidelity/stability **metrics slice → `aaanalysis/metrics/`** (in-core); DL-XAI bench → ProtXplain. |
| 47 | 3 | v2.X | 🔄 | Feature attribution → CPP scale space. |
| 48 | 3 | v2.X | 🔄 | Concept-based: cluster CPP scales (AAclust). |
| 265 | 3 | — | 🔄 | Symbolic-regression surrogate for fitness rules; heavy-dep, research. |
| 49–54 | 3 | v2.X | 🔄 → ProtXplain | example-based / rule / neural / surrogate / uncertainty-aware / causal XAI — heavy deps; keep out of core (`pro-core-boundary.md`). #53 merges intent with #16. |

## Defer / Reject appendix (with cited rule)
- **#64 ⏸️** — deferred by **ADR-0012**; blocked on #65 + the pro move.
- **v2.X XAI suite (#47–#55) / #265 / #54 / #51** — not rule-violations, but balloon the
  dependency surface (captum/DoWhy/PyMC/alibi/imodels) or belong to **ProtXplain**
  (`pro-core-boundary.md`). Keep the boundary; #55's *metrics* slice stays in-core.
- **#273 / #263 / #261-`deap`** — new-dependency asks → `[omics]`/`[gpu]`/`[dev]` extras,
  CONFIRM-FIRST `pyproject.toml`; never a required core dep.
- No issue currently triggers a hard-rule **Reject** (no `AAanalysisError`/SECURITY.md/ruff/mypy asks).
